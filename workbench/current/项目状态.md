# Personal Workbench｜项目状态

状态：current  
更新时间：2026-09-06  
阶段：**Stage 1｜TA-1A First Usable Implementation — AUTHORIZED FOR DISPATCH**

## Current Goal

基于已经通过的 V0 Route Freeze，由 `04｜Agent 任务调度` 为 **TA-1A First Usable Core Vertical** 形成正式 executable Task Packet，并进入受控 implementation。

## Current Gate

**Stage 0 Route Freeze：CLOSED / PASS**

| Gate | 当前状态 | Owner / 主责 |
|---|---|---|
| Product Direction Decision | **PASS / APPROVED** | Owner + 00 |
| G0.1 Product Baseline | **PASS** | 01 → Owner → 00 |
| G0.2 Scope | **PASS** | 01 → Owner → 00 |
| G0.3 Draft Task Axis | **PASS** | 03 + Owner → 00 |
| G0.4 Reference Audit + Feasibility | **PASS** | 02 → 00 |
| G0.5 Revised Route / Architecture | **PASS** | 03 + Owner → 00 |
| G0.6 Owner Route Freeze | **PASS / APPROVED** | Owner + 00 |

## G0.6 Route Freeze Decision｜2026-09-06

Owner 已明确批准当前 Revised Route / Architecture，并接受 Owner-machine tray focused check 后移到 TA-1A Exit 前完成。

00 正式记录：

```text
Route Freeze = PASS
Implementation = AUTHORIZED
Current Implementation Target = TA-1A First Usable Core Vertical
```

正式 Decision Record：

`../decisions/D-003_V0RouteFreeze与TA-1AImplementationAuthorization.md`

## Frozen Route / Architecture

当前 frozen sources：

- `current/产品定义.md`；
- `current/开发路线.md`；
- `architecture/架构方案.md`；
- `decisions/D-002_正式Pivot到PersonalStateCore.md`；
- `decisions/D-003_V0RouteFreeze与TA-1AImplementationAuthorization.md`。

核心冻结内容：

- Product：**Personal State / Information & Action Desktop**；
- CORE：`Today / Plan / Tracks`；
- V0：`Thin Tracks + Usable Plan + Derived Today`；
- Desktop Host：**Electron**；
- Canonical persistence：**SQLite**；
- Tracks / Plan authoritative，Today 主要 derived；
- Today acknowledgement 为极窄当日视觉确认；
- stable immutable identity；
- occurrence identity = `SeriesStableID + OriginalScheduledOccurrenceKey`；
- date-only / local wall-time / instant 分离；
- Current Vector `[startDate, endDate]` inclusive + no-overlap；
- recurrence = series + dynamic occurrence + exception；
- whole-series edit 清除旧 exceptions 前警告 / 确认；
- consistent snapshot backup；
- validated staged restore + pre-restore safety snapshot；
- schema version + ordered transactional migration + upgrade safety backup；
- system tray V0 Required；close → tray；tray restore / real exit；
- First Usable = **TA-1A**；TA-1A 后立即 Owner real-data UAT；
- Complete V0 = **TA-1B**。

## TA-1A Authorized Scope

### Tracks

- Thin Track 完整字段；
- create / edit；
- Active / Completed / Archived；
- immutable Track ID。

### Plan

- Current Vector；
- Vector overlap hard rejection；
- 单次 Scheduled Item；
- optional Track Link；
- 一个真正可用的 Month View。

### Today

- 默认入口；
- 当前 Current Vector；
- 今日单次日程；
- 当日 acknowledgement；
- 不展示全部 Tracks / Unscheduled / 未来预告 / scoring。

### Persistence / Desktop

- Electron packaged Windows app；
- SQLite canonical DB；
- transactional durable write / restart；
- manual backup / restore；
- restore safety snapshot / validation；
- schema version + migration baseline；
- upgrade safety backup；
- system tray；
- close → hide；
- tray restore / real exit。

## TA-1A Exit Residual

以下 focused check 已获 Owner 接受，不再阻塞 Route Freeze，但**必须在 TA-1A Exit 前完成**：

```text
Owner Windows machine
→ notification-area icon 实际可见
→ 鼠标点击恢复 / focus
→ close → hide to tray
→ tray real exit
```

如果失败，TA-1A 不得 Exit；应由 05 做 Independent Review / Root Cause，并在必要时回流 03。

## Not Authorized Yet

以下不因 Route Freeze 自动开工：

- TA-1B 在 TA-1A Review / Owner UAT 之前的实现；
- recurrence；
- Reminder；
- Memo；
- Unscheduled；
- Week View；
- Milestones；
- AI-assisted Track Update；
- Google Calendar sync；
- Custom Functions；
- Launch Target；
- Knowledge / Habit / Finance / Health；
- AI Collaboration 恢复；
- Plugin / SDK / Page Builder；
- recurrence “这一次及以后”；
- multi-device / cloud sync。

## Active Work

- `00｜项目总控`：已完成 G0.6 Route Freeze 与 Decision Propagation；
- `04｜Agent 任务调度`：**当前下一主线**，为 TA-1A 形成正式 executable Task Packet；
- `01`：Product maintenance；
- `02`：Research standby；
- `03`：Frozen Route / Architecture maintenance；只有实现证据要求路线回流时重新介入；
- `05`：等待 TA-1A implementation evidence，之后承担 Independent Review / Reality Gate / Owner UAT。

## Next Recommended Action

进入 **`04｜Agent 任务调度`**。

04 应读取 frozen `current/开发路线.md`、`architecture/架构方案.md` 和本 `项目状态.md`，为 **TA-1A First Usable Core Vertical** 生成正式 Task Packet，并明确 acceptance / validation / Git return / 05 review handoff。

当前：

**Implementation = AUTHORIZED FOR TA-1A DISPATCH。**
