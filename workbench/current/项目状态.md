# Personal Workbench｜项目状态

状态：current  
更新时间：2026-09-06  
阶段：**Stage 0｜Pre-Implementation Alignment — ACTIVE**

## Current Goal

完成 **G0.4 High-risk Feasibility**：在已经完成 Pass 1 / Pass 2 Reference Audit 的基础上，执行报告判定为必要的最小 Technical Spikes，关闭 Route Freeze 前必须有现实证据的高风险问题。

## Current Gate

**Stage 0 / Route Freeze Gate：OPEN**

| Gate | 当前状态 | Owner / 主责 |
|---|---|---|
| Product Direction Decision | **PASS / APPROVED** | Owner + 00 |
| G0.1 Product Baseline | **PASS** | 01 → Owner → 00 |
| G0.2 Scope | **PASS** | 01 → Owner → 00 |
| G0.3 Draft Task Axis | **PASS** | 03 + Owner → 00 |
| G0.4 Reference Audit + Feasibility | **REFERENCE AUDIT COMPLETE / REQUIRED SPIKES OPEN** | 02｜参考审计与技术研究 |
| G0.5 Revised Route / Architecture | WAITING FOR G0.4 SPIKE EVIDENCE | 03 + Owner → 00 |
| G0.6 Owner Route Freeze | BLOCKED BY G0.4 + G0.5 | 00 + Owner |

## G0.4 Audit Review｜2026-09-06

`research/G0.4_Reference_Audit_Report.md` 已完成 Pass 1 + Pass 2、Evidence Matrix、Supporting / Contradicting Findings、Route-changing Findings 与 G0.5 Handoff。

00 判断：**研究文档部分合格，但 G0.4 Gate 尚不能 PASS。**

原因：02 已明确判定以下问题仅靠文档不足，Route Freeze 前需要现实 Spike evidence：

### S1｜Packaged Desktop Lifecycle

比较 Electron / Tauri 的最小 packaged Windows 行为：

```text
launch
→ close window / hide
→ tray remains
→ tray restore / focus
→ real exit
→ relaunch
→ stable data path
```

### S2｜SQLite Durability / Backup / Migration

最小验证：

```text
write → kill → restart → verify
backup → destructive mutation → restore → verify
schema fixture → migrate → verify
integrity validation
```

### S3｜Recurrence Identity / Exception

最小 domain prototype：

```text
series
→ occurrence
→ edit/delete only this occurrence
→ moved occurrence keeps original identity
→ Today acknowledgement
→ restart projection
→ date/local-time boundary tests
```

这些 Spike 必须保持：`EXPLORATION / NOT CANONICAL ARCHITECTURE / NOT PRODUCTION COMMITMENT`。

## Research Findings Awaiting G0.5 Adoption

以下是 **research proposals**，尚未自动成为 current Route / Architecture：

1. **RC-01**：把 `TA-1 Complete V0 → First UAT` 改为 `TA-1A First Usable → Owner UAT → TA-1B Complete V0`；
2. **RC-02**：Persistence / backup / migration proof 提前到 Route Freeze 前；
3. **RC-03**：Desktop Host 不能凭文档 Freeze，需要 packaged lifecycle Spike；
4. **RC-04**：G0.5 必须关闭 whole-series edit + existing exceptions 的 deterministic policy；
5. SQLite 是 preferred persistence candidate，但尚未冻结；
6. Occurrence identity 建议使用 `SeriesStableID + OriginalScheduledOccurrenceKey`；
7. Month + Week Calendar 不必同时阻塞 First Usable；
8. Memo 不应阻塞 First Usable；
9. Restore 前 safety snapshot + validation 建议加入极窄 recovery guardrail。

这些建议只能在 G0.4 完成后由 `03 + Owner` 讨论并形成 G0.5 Revised Route / Architecture Candidate。

## Current Approved Product / Route Constraints

- 当前产品方向：**Personal State / Information & Action Desktop**；
- CORE = `Today / Plan / Tracks`；
- V0 = `Thin Tracks + Usable Plan + Derived Today`；
- Workbench 自己拥有 Core Personal State；
- Today 主要是 derived view，不复制 Plan / Tracks 的业务事实；
- Today “已完成”只是当天视觉确认，不推进 Plan / Track，不做完成率 / streak / 评分；
- Current Vector 日期区间禁止重叠；
- recurrence 采用 series definition + dynamic occurrence + exception；
- recurrence edit / delete V0 只支持“仅这一次 / 整个循环”；
- 核心数据本地可靠保存；V0 需要 restart recovery、手动 backup / restore 与 migration；
- system tray 属于 V0 Required；开机启动 Deferred；
- AI / Milestones / Sync / Custom / Plugin / SDK 等继续 Deferred；
- Route Freeze 前正式 implementation 未授权。

## Active Work

- `00｜项目总控`：已完成 G0.4 Research Audit 文档审查与状态更新；
- `02｜参考审计与技术研究`：**继续当前主线**，执行 S1 / S2 / S3 并持久化 Spike evidence；
- `03｜架构与开发路线`：等待完整 G0.4 evidence；
- `01`：维护状态；
- `04 / 05`：无正式 implementation / review 工作。

## Next Recommended Action

继续在 **`02｜参考审计与技术研究`** 完成 S1 / S2 / S3。

Spike 完成后回到 00；若现实证据足够关闭高风险问题，则 00 判定 G0.4 PASS，并正式流转到 03 做 G0.5 Revised Route / Architecture。

当前：

**Implementation = NOT AUTHORIZED**。