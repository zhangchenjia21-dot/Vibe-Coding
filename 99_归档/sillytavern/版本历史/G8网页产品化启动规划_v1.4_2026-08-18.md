# G8｜网页产品化启动规划 v1.4

状态：`ACTIVE`  
日期：2026-08-18  
上游：G1–G7 PASS / CLOSED  
当前任务：`G8-WEB-05｜Technical Migration Closure`

> 本文件 **SUPERSEDES** `G8网页产品化启动规划_v1.3_2026-08-18.md`。
>
> v1.4 记录 `G8-WEB-04 Final Host Convergence` 已由 `sillytavern@5c76f4302152a7598b54d7f9d5774616b1fd618d` 完成并独立审核 PASS，同时根据第 15 号 Runtime Context Orchestration 核心裁定执行 Decision Propagation，更新 G9 Task DAG。

---

## 1. 当前 G8 状态

### G8-WEB-01｜Product UI Contract Freeze
**PASS / CLOSED**

### G8-WEB-02｜Product Shell / Visual Language
**PASS / absorbed**

### G8-WEB-03｜Live Product Session Integration
**PASS / CLOSED**

### G8-WEB-04｜Runtime-extensible UI Host
**PASS / CLOSED**

正式基线：

`zhangchenjia21-dot/sillytavern@5c76f4302152a7598b54d7f9d5774616b1fd618d`

已证明：

- typed handwritten UI Definition 可进入正式 Product Bootstrap；
- Core-only 默认行为保持；
- Extension Surface / secondary View / 9 类 Host Capability 正式成立；
- Declarative Structure 与 player-safe live data 分离；
- arbitrary state path / expression / frontend execution 禁止；
- live Contribution 会随 Formal Turn 后的 public state 重新物化；
- Creation Contribution 对齐 `expansion_settings / character / opening`；
- sourceDefinitionId / ownerGameplayId 可追踪；
- gameplay off → dormant → on restore；
- Surface Order 使用独立 durable Product UI Preference，跨应用重启保存，Restore 不回滚；
- invalid Host 全量 fail closed。

正式产品架构依据：

`14_酒馆游戏_G8_RuntimeExtensibleUI产品架构裁定_v1.2_2026-08-18.md`

### G8-WEB-05｜New Game / API / Settings

产品主路径：**PASS**。

当前剩余仅为架构迁移尾项：

> **Technical Migration Closure**

目标：退休旧 One Draft / Five Sections production authority，使正式 New Game authority 唯一化为 Creation Project。

### G8-WEB-06｜Save Center + Recovery/Error UX

**DEFERRED / NON-BLOCKING FOR G9**。

底层 G6/G7 authority 已关闭；当前个人开发阶段不要求完整玩家产品入口。

### G8-WEB-07｜Correction / Undo / Re-input

**DEFERRED / NON-BLOCKING FOR G9**。

Alpha / G11 前重新拉回 Required。

### G8-WEB-08｜Controlled Multi-action Core Slice

**REQUIRED**。

只冻结会影响未来 Expansion Action Intent / Resolution 的最小 Core semantics；不建设万能 Planner / workflow DSL。

### G8 Exit Gate（原 WEB-09）

**REQUIRED**，但不作为一轮独立大型 UI 开发。

WEB-05 与 WEB-08 通过后执行当前关键路径 E2E / regression / narrow / keyboard / Stage UAT；只有发现 P0/P1 才开修复任务。

---

## 2. 当前最短 G8 关键路径

```text
WEB-04 Final Host Convergence             ✓ PASS / CLOSED
↓
WEB-05 Technical Migration Closure        ← NEXT
↓
WEB-08 Controlled Multi-action Core Slice
↓
G8 Exit Gate
↓
G8 PASS / CLOSED
```

WEB-06 / WEB-07 不在当前关键路径。

---

## 3. WEB-05 Technical Migration Closure 的必要性

当前 exact-SHA `5c76f430…` 仍能看到 production dual path：

- `ProductFoundationClient` 仍暴露 `createDraft / loadDraft / editDraftField / deferDraftField / generateDraft / createGame`；
- `GameCreationPublicApi` 仍维护 `GameCreationDraftStore / GameCreationContentFlow / GameInstanceCreationFlow`；
- legacy Draft / Five Sections types 与 exports 仍存在；
- HTTP draft routes 与 obsolete tests 仍需集中退休。

Host Creation bridge 已经从 Five Sections 脱离，因此现在是执行 Supersession cleanup 的正确时机。

目标状态：

```text
Product New Game Authority
=
Creation Project only
```

同时保留 `use_my_assets` 的 G9-ready product concept，不因这个概念保留 legacy Draft architecture。

---

## 4. Decision Propagation：第 15 号核心裁定对 G9 Task DAG 的影响

第 15 号：

`Runtime Context Orchestration 与模块化复杂度控制裁定 v1.0`

已经冻结：

```text
Asset Library
!= Game Enabled Asset Set
!= Current Runtime Relevant Set
!= Current Model Visible Working Set
```

以及目标链：

```text
Player Input
+
Enabled Expansion Directory
+
minimal current scene summary
↓
Router Model
↓
Expansion / Domain / Intent candidates
↓
Program structural validation
+
state-mandatory augmentation
↓
Current Relevant Set
↓
JIT bounded Runtime Projection
↓
Semantic / Resolution / Narrative consumers
```

该决策**不要求回头在 G8 临时发明 Expansion Router**，但它改变 G9 的正式任务顺序。

进入 G9 后，不再采用：

```text
Compatibility Audit
→ asset-spec Schema Freeze
```

而应采用：

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding + Context Orchestration Foundation
      - handwritten/internal Expansion runtime profiles
      - Enabled Capability / Expansion Directory
      - Router Model contract
      - Program structural validation
      - state-mandatory augmentation
      - Current Relevant Set
      - bounded JIT Runtime Projection
      - Context Contract vertical proof
↓
G9-03 asset-spec vNext Machine Contract
      - 只声明已被 Runtime vertical proof 验证的 routing / activation / context / handoff / host requirements
↓
G9-04 Game Asset Adapter / Compiler
↓
G9-05 Creator rebuild
↓
Golden Asset Family migration / validation
```

原则：

> **Runtime Context Orchestrator Before Asset Context Schema。**

与 G8 的：

> **Host Before Protocol。**

属于同一治理原则。

---

## 5. G9-02 不得提前做什么

G9-02 仍只使用 internal typed / handwritten profiles 验证 Runtime，不提前冻结外部 JSON Schema。

不得在该 Foundation 里：

- 扫描 Markdown；
- 让 asset-spec 反向决定 Runtime；
- 使用任意 prompt include graph；
- 把整个 Enabled Expansion 全文塞入模型；
- 建通用 expression/query DSL；
- 让 Router 成为玩家动作白名单；
- 让模型拥有正式 world commit authority。

---

## 6. 当前 Next

现在唯一正式下一项：

> `G8-WEB-05｜Technical Migration Closure`

其后：

> `G8-WEB-08｜Controlled Multi-action Core Slice`

尚不开始 G9 machine contract 实现。
