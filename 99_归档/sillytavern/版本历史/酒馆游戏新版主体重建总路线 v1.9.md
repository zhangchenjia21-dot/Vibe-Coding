# 酒馆游戏新版主体重建总路线 v1.9

> 状态：**当前正式路线**
> 日期：2026-08-18
> 新版游戏仓库：`D:\AI\Projects\sillytavern`
> **SUPERSEDES** `酒馆游戏新版主体重建总路线 v1.8.md`。

---

## 0. 总路线

```text
G1 真实模型兼容性                 PASS / CLOSED
↓
G2 最小世界运行内核               PASS / CLOSED
↓
G3 正式世界能力垂直扩展           PASS / CLOSED
↓
G4 程序判定与 Dice                PASS / CLOSED
↓
G5 长期世界连续性                 PASS / CLOSED
↓
G6 Save / Continue / Restore      PASS / CLOSED
↓
G7 Crash / Resume / Recovery      PASS / CLOSED
↓
G8 网页产品化                     ACTIVE
↓
G9 资产生态接入                   BLOCKED BY G8 EXIT
↓
G10 Provider 扩展                 FUTURE
↓
G11 Alpha / 全系统验收            FUTURE
↓
G12 Windows Release               FUTURE
```

总原则：

> **先证明真实纵向链，再横向扩展；先证明 Host / Runtime Foundation，再让外部协议声明这些能力。**

---

# 1. G1–G7

G1–G7 已正式 PASS / CLOSED。除其收口文档定义的 P0/P1 authority violation 外，不因 G8/G9 产品或资产需求重开。

---

# 2. G8｜网页产品化

## 2.1 已完成

- WEB-01 Product UI Contract Freeze：PASS / CLOSED
- WEB-02 Product Shell / Visual Language：PASS / absorbed
- WEB-03 Live Product Session Integration：PASS / CLOSED
- WEB-04 Runtime-extensible UI Host Final Convergence：PASS / CLOSED
- WEB-05 New Game / API / Creation：**PASS / CLOSED**

WEB-04 基线：`5c76f4302152a7598b54d7f9d5774616b1fd618d`

WEB-05 Technical Migration 基线：`61d8af56bd52e127de29f113c524681f0eea819a`

当前 New Game authority：

```text
Creation Project only
```

旧 One Draft / Five Sections production path 已退休。

## 2.2 当前 Next

### G8-WEB-08｜Controlled Multi-action Core Slice

状态：**NEXT / REQUIRED**。

目标仅冻结最小 Core semantics：

- 一个 Player Input 可表达少量顺序明确的动作序列；
- sequence bounded；
- 每个子动作继续受 Capability / Authorization / Resolution 控制；
- Program 拥有顺序、时间推进与最终提交；
- 一个 Player Input 最多一个 Formal Turn；
- 全序列 atomic；
- no partial commit；
- Narrative 只叙述 Program 已决定的 sequence outcome；
- 不建设万能 Planner / workflow DSL / autonomous agent loop。

### WEB-06 Save Center + Recovery/Error UX

**DEFERRED / NON-BLOCKING FOR G9**。

### WEB-07 Correction / Undo / Re-input

**DEFERRED / NON-BLOCKING FOR G9**。

### G8 Exit Gate

WEB-08 完成后直接执行当前关键路径回归 / Product E2E / Desktop+narrow / keyboard+focus / GitHub-first Review / 项目所有者 Stage UAT。只有 P0/P1 才新增修复任务。

## 2.3 当前 G8 最短关键路径

```text
WEB-04 Host                              ✓ CLOSED
↓
WEB-05 Creation Migration                ✓ CLOSED
↓
WEB-08 Controlled Multi-action           ← NEXT
↓
G8 Exit Gate
↓
G8 PASS / CLOSED
```

---

# 3. G9｜资产生态接入

G9 在 G8 CLOSED 前不开始正式 machine contract 实现。

## 3.1 G9-01 Compatibility Audit

先审现有 `tavern-asset-spec` / `tavern-creator`：

- A｜纯领域协议可保留；
- B｜概念正确、旧 Adapter 失效；
- C｜被旧 Game implementation 污染，才升级。

## 3.2 G9-02 Runtime Asset Binding + Context Orchestration Foundation

**必须先于 asset-spec vNext Schema Freeze。**

第 15 号 current：

`15_酒馆游戏_RuntimeContextOrchestration与模块化复杂度控制裁定_v1.1_2026-08-18.md`

基础集合：

```text
Asset Library
!= Game Enabled Asset Set
!= Runtime Relevant
!= Model Visible Working Set
```

对大型模块化 Package：

```text
Package Included
!= Feature Enabled
!= Module Enabled
!= Runtime Relevant
!= Model Visible
```

G9-02 用 internal handwritten profiles 先证明：

```text
Player Input
+
pruned Enabled Expansion / Feature / Module Directory
+
minimal Current Scene / Active Context Summary
↓
Router Model
↓
immediate relevant capability / intent candidates
↓
Program structural validation
+
state-mandatory augmentation
↓
Current Relevant Set
↓
bounded JIT Runtime Projection
```

同时证明：

```text
Background deterministic progression
→ Program-only
→ zero model call
```

以及：

```text
Module OFF
→ no routing profile
→ no conditional dependency
→ no module state / surface
```

Router 不预测所有下游；正式 Event / typed Handoff 再激活 downstream consumers。

原则：

> **Runtime Context Orchestrator Before Asset Context Schema。**

## 3.3 G9-03 asset-spec vNext

只在 G9-02 vertical proof 后冻结 machine contract。asset-spec 只能声明已经被 Runtime 证明的 identity / dependency / activation / routing / context / handoff / Host requirement / information boundary。

不得通过 Schema 发明 arbitrary frontend execution、state path、general expression/query DSL、direct Game State mutation 或 Enabled Asset 全文自动入 Prompt。

## 3.4 G9-04 Game Asset Adapter / Compiler

```text
Validated Asset
→ Runtime Definition
→ Runtime UI Definition
→ Runtime Context Contract
→ Game Assembly
```

## 3.5 G9-05 Creator rebuild

基于已冻结 machine contract 重建 Creator；Creator 不拥有 Runtime truth，也不发明未验证 Host / Runtime capability。

---

# 4. G10–G12

G10：第二 Provider 在 G9 稳定后。

G11：重新拉回 WEB-06 / WEB-07、深度 Responsive / Accessibility、Context Composition stress、完整 Product polish 与 Alpha UAT。

G12：Windows 打包、安装/升级、配置迁移、诊断与发布验收。

---

# 5. 当前工程原则

1. Single Authority。
2. Shared Foundation Before Consumers。
3. Host Before Protocol。
4. Runtime Context Orchestrator Before Asset Context Schema。
5. Declarative Structure != Live Data Access。
6. Supersession Before Compatibility。
7. Dependency Graph != Context Inclusion Graph。
8. Package Included != Feature Enabled != Module Enabled != Runtime Relevant != Model Visible。
9. Background deterministic progression != Model Activation。
10. Program owns state / permission / RNG / resolution / commit / recovery。
11. Model role bounded to semantic interpretation / advisory / narrative。
12. Product UX 可按 personal / Alpha / Release 成熟度分级。
13. Push != PASS；正式实现需 GitHub-first Independent Review。

---

# 6. 当前 Next

> **G8-WEB-08｜Controlled Multi-action Core Slice**

完成并独立审核 PASS 后：

> **G8 Exit Gate**

尚未允许正式开始 G9 Context Orchestration implementation、asset-spec vNext、Creator rebuild 或 external Asset Adapter。