# G8｜阶段收口与 G9 前置裁定 v1.5

状态：`CURRENT DECISION`  
日期：2026-08-18  
supersedes: `G8阶段收口与G9前置裁定_v1.4_2026-08-18.md`

> v1.5 记录 `G8-WEB-08 Controlled Multi-action Core Slice` 已由 `sillytavern@3ad5b419e9fb289488a18ded82c5a77f2cdd376c` 完成并经 GitHub-first Independent Review 判定 PASS。当前 G8 不再有新的 Required 功能开发项，进入最终 Exit Gate。

## 1. 当前阶段事实

- G1–G7：PASS / CLOSED。
- G8-WEB-01～03：PASS / CLOSED / absorbed。
- G8-WEB-04 Final Host Convergence：PASS / CLOSED。
- G8-WEB-05 Technical Migration Closure：PASS / CLOSED。
- G8-WEB-08 Controlled Multi-action Core Slice：**PASS / CLOSED**。
- G8-WEB-06 Save Center / Recovery Product UX：DEFERRED / NON-BLOCKING。
- G8-WEB-07 Undo / Re-input Product UX：DEFERRED / NON-BLOCKING。
- 当前唯一下一项：**G8 Exit Gate**。
- G9：NOT STARTED / blocked by G8 Exit Gate。

## 2. WEB-08 已冻结的 Core Semantics

正式基线：

`zhangchenjia21-dot/sillytavern@3ad5b419e9fb289488a18ded82c5a77f2cdd376c`

已证明：

```text
one Player Input
→ bounded ordered action_sequence
→ Program authorization / transient preflight
→ one aggregate FormalDelta
→ one atomic commit
→ one Formal Turn
```

当前 G8 capability limit：

- sequence = 2 steps；
- 只允许现有 deterministic executable atomic actions；
- 任一步需要 Dice / Resolution → 整段在 RNG 前 fail closed；
- no partial commit；
- one idempotency key / one DurableExecutionRecord；
- semantic/formal/narrative crash recovery exactly-once；
- background deterministic progression 只在 aggregate time 上 Program-owned 执行一次；
- Product UI 保持一条玩家输入、一个 Formal Turn，不增加 Planner UI。

该限制是 internal G8 capability boundary，不是未来 asset protocol 永久上限。

## 3. Independent Review 结论

PASS 依据包括：

- `action_sequence` 只存在于 Runtime-only semantic extension，不修改冻结 `player-intent-candidate.v0`；
- `PlayerActionAuthorization.authorizedActions[]` 继续是唯一授权事实源；
- sequence evidence 必须 non-empty / unique / raw-input substring / ordered；
- transient sequence composer 不持有 Store / Provider / RNG；
- transient reducer 不推进 revision / turn，不写 Event / SQLite；
- aggregate delta 保留 initial→final canonical effect 与 ordered public events；
- second step 在 transient state 上重新验证 capability 与 resolution necessity；
- resolution-required sequence 在 RNG 前拒绝；
- focused regression 覆盖 atomic failure、background once、schema bounds、alternative/question、atomic omission guard、evidence failure、idempotent replay、三阶段 crash recovery、Product one-turn timeline；
- targeted real DeepSeek smoke 已由执行报告证明 positive / negative PASS。

GitHub commit 无独立 CI status，因此执行者报告的本地测试数字作为 Gate evidence；PASS 主要建立在 exact-SHA code/test review + real smoke report 上。

## 4. G8 当前 Required 开发项已完成

```text
WEB-04 Host Convergence              PASS
WEB-05 Creation Migration Closure    PASS
WEB-08 Multi-action Core Slice       PASS
```

不再在 G8 Exit 前新增：

- Save Center；
- Undo / Re-input；
- Context Router；
- asset-spec；
- Creator；
- resolved multi-action；
- deep mobile / WCAG polish。

除非 Exit Gate 发现 P0 / P1 blocker。

## 5. G8 Exit Gate

Exit Gate 不是新功能开发轮次。

必须核验：

1. Desktop 主路径无 P0/P1 blocker；
2. narrow 关键路径无 P0/P1 blocker；
3. keyboard / focus 无明显 blocker；
4. no-key / configured-key；
5. Creation Project → Game Instance → Product Session；
6. Host production vertical；
7. Controlled Multi-action production vertical；
8. G5–G7 regression；
9. disclosure / hidden-private boundary；
10. full tests / typecheck / lint / product build / launcher smoke；
11. GitHub-first final code/route review；
12. 项目所有者一次 G8 Stage UAT。

只有 Gate 发现 P0/P1 才开集中修复任务；P2/P3 进入后续 backlog。

## 6. G9 当前正式 Task DAG

第 15 号核心 current = `Runtime Context Orchestration v1.1`。

G8 CLOSED 后：

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding + Context Orchestration Foundation
      - pruned Enabled Package / Feature / Module Directory
      - Model-first immediate routing
      - Program structural validation
      - state-mandatory augmentation
      - bounded JIT Runtime Projection
      - disabled Module fail-closed
      - conditional dependency activation
      - background deterministic progression with zero model call
↓
G9-03 asset-spec vNext Machine Contract
↓
G9-04 Game Asset Adapter / Compiler
↓
G9-05 Creator rebuild
```

原则继续冻结：

> Host Before Protocol。  
> Runtime Context Orchestrator Before Asset Context Schema。

## 7. 当前 Next

唯一正式下一项：

> **G8 Exit Gate**

Gate PASS + 项目所有者 Stage UAT PASS 后：

> **G8 PASS / CLOSED → G9-01 Compatibility Audit**
