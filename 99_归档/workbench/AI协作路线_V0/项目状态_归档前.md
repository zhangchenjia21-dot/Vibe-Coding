# Personal Workbench｜项目状态

状态：current  
更新时间：2026-09-06  
阶段：**Stage 0｜Pre-Implementation Alignment — ACTIVE**

## Current Goal

在已经通过的 Product Baseline / Scope 基础上，由 Owner 与 `03｜架构与开发路线` 先完成对 Draft Task Axis / Architecture Questions 的真实讨论与审核，再决定是否形成 G0.3 正式 submission；之后才进入 Reference / Technical Audit。

## Current Blocker

1. 03 已生成一版 Draft Task Axis 与 Architecture Questions Register，但**未经 Owner 讨论 / 审核**；
2. 草案中的若干路线判断曾被写成 `DECIDED`，在 Owner 审核前不能视为生效约束；
3. G0.3 尚未形成经过 Owner 审核的正式 submission；
4. Reference Audit Pass 1 + Pass 2 尚未开始；
5. TV-01～TV-05 尚未取得足够技术证据；
6. Revised Task Axis / 必要 Architecture 尚未形成；
7. Route Freeze 尚未通过，因此正式 implementation 未授权。

## Current Gate

**Route Freeze / Product Definition Gate：OPEN**

| Gate | 当前状态 | Owner / 主责 |
|---|---|---|
| G0.1 Product Baseline | **PASS** | 01 → 00 |
| G0.2 Scope | **PASS** | 01 → 00 |
| G0.3 Draft Task Axis | **WORKING DRAFT EXISTS / OWNER DISCUSSION REQUIRED** | 03 + Owner → 00 |
| G0.4 Reference Audit Pass 1 + 2 | WAITING FOR OWNER-REVIEWED G0.3 SUBMISSION | 02｜参考审计与技术研究 |
| G0.5 Revised Route / Architecture | BLOCKED BY G0.3 + G0.4 | 03 + Owner → 00 |
| G0.6 Owner Route Freeze | BLOCKED BY ABOVE | 00 + Owner |

### Governance Correction｜2026-09-06

03 在 G0.3 中自行生成并写入了 Draft Task Axis / Architecture Questions，随后将其描述为已形成并准备交由 00 Gate Review，但中间没有先与 Owner 讨论和取得明确批准。

00 裁定：**该流程不符合本项目希望采用的 Owner-controlled planning 模式。**

从现在开始强制执行 `workbench/AGENTS.md` 的 **Owner Discussion & Promotion Gate**：

```text
Working Draft
→ Owner Discussion
→ Owner explicit approval
→ GitHub Promotion
→ 00 Gate Review
```

Owner 沉默不构成批准；`DRAFT` 标签不构成 GitHub Promotion permission；专业聊天框不能自行宣布 Gate 完成。

此次 03 已生成内容不删除，但已降权至 `workbench/discussion/` 作为未审核草案，不构成 current route / Architecture Authority / G0.3 submission。

## Latest Approved Decisions

- 项目处于 `Stage 0｜Pre-Implementation Alignment`；
- 用户是最终 Product Owner 与最终 UAT 裁定者；
- `Vibe-Coding/workbench/` 是 Product / Architecture / Roadmap / Decision / Status / Research 的 canonical workspace；
- `zhangchenjia21-dot/Workbench` 是代码、测试、runtime、repository-native executable Task 与 implementation review 的事实源；
- V0 Primary Purpose：减少 Owner 在 AI Coding 协作中的机械协调劳动，而不是自研更聪明的新 AI；
- V0 Core Loop：`Discuss → Select → Execute → Push → Review → Fix / Accept`；
- Owner 通过 **Selected Text** 明确执行意图，V0 不要求 GPT 特殊 Task 格式；
- V0 第一条真实纵向采用 **Codex-first**，Grok / Kimi 等多 Agent 横向能力 Deferred；
- Agent Completion Report 不构成最终事实，进入 Review Handoff 前需要 GitHub 中存在可审核实现；
- V0 至少提供 **One-click GPT Review Handoff**，不要求第一版实现全自动 Review；
- 自动 Routing、Multi-Agent orchestration、Quota Dashboard、复杂 Task Management、Knowledge / Today / Plan / Tracks 等均不阻塞 V0；
- **重大 Product / Architecture / Roadmap / Task Axis / Gate 内容必须 Discussion First，Owner 明确批准后才能 Promotion 到 authoritative GitHub source；**
- Route Freeze 前不授权正式大规模编码，低成本 Spike / Prototype 必须明确标记 exploration。

## Open Decisions / Questions

以下仍不是 current architecture，应先由 Owner 与 03 讨论，再决定哪些成为 G0.3 正式问题集：

- TV-01：现有 ChatGPT 工作流在 Workbench 中如何可靠承载；
- TV-02：如何可靠取得 Owner 在 GPT 回复中选中的文本并触发 Handoff；
- TV-03：如何向当前 ChatGPT conversation 发起可靠的一键 Review Handoff；
- TV-04：如何真实启动 Codex 并可靠获取 Running / Completed / Failed、日志和 Completion Report evidence；
- TV-05：如何证明目标 implementation 已 commit / push 到预期 GitHub repository / ref；
- V0 的明确平台 / compatibility / packaging Hard Constraints；
- DeepSeek Harness 的 Build / Fork / Extend / Reference / Reject 定位；
- Desktop / Web / Local architecture 与最终技术栈；
- 进程 ownership、状态模型、持久化、失败恢复、安全边界；
- 哪些 contract / abstraction 必须等第一个真实 consumer 后再冻结。

## Active Work

- 00：完成本次治理纠偏，并将未经 Owner 审核的 03 草案降权；
- 03：下一步应**与 Owner 讨论草案本身**，逐项解释关键路线判断、备选方案、风险与需要 Owner 裁定的问题；
- 02：暂不启动正式 G0.4；等待 Owner 审核后的 G0.3 submission；
- 01：产品基线维护状态。

## Deferred / Not Authorized Yet

- 正式功能实现；
- 大规模项目脚手架；
- 正式 Agent implementation Task；
- Grok / Kimi 等多 Agent 横向接入；
- 自动 Agent Routing / Multi-Agent orchestration；
- 长期外部协议 / SDK / Plugin platform；
- 因历史设想而直接绑定 DSH 或任一具体框架。

## Next Recommended Action

回到 **`03｜架构与开发路线`**，但不是让它继续写文档或交给 00。

要求 03：

1. 把已经生成的未审核草案视为 `WORKING DRAFT`；
2. 停止 GitHub Promotion；
3. 在聊天中先向 Owner 解释整体 Task Axis 和最关键的 5～10 个路线判断；
4. 对每个重大判断明确区分 `PROPOSED / HYPOTHESIS / OPEN / DEFERRED`；
5. 与 Owner 逐项讨论需要产品或架构裁定的问题；
6. 只有 Owner 明确表示“这版可以作为 G0.3 submission / 写回 GitHub”后，才更新 current / architecture；
7. 然后回到 00 做 G0.3 Gate Review。
