# current｜当前治理入口

这里仅保存 The World 治理层的 rolling canonical；实现事实仍由 `zhangchenjia21-dot/the-world` 持有。

- `GPT项目动态路由.md`：AI / ChatGPT 读取项目时的动态路由。
- `延后裁定与待办.md`：正式 deferred / backlog Owner。

更新这两个文件时直接原位修改。不要再创建带 `_CURRENT`、版本号或日期的新权威副本。

这里不复制实现仓库的 HEAD、代码状态、游戏事实或产品规格。
