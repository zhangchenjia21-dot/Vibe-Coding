---
title: 任务包｜判定与检定过渡包提交与生效
status: current
version: 1.0
created: 2026-08-24
task_id: TW-ASSET-2026-08-24-01
task_type: integration-commit
scope: the-world-project
---

# 任务包｜判定与检定过渡包提交与生效

## 1. 任务标识

- Task ID：`TW-ASSET-2026-08-24-01`
- 类型：integration-commit（验证 + 提交已落地文件，无新开发）
- Owner：执行线程（the-world 本地工作区执行 Agent）
- Repo / branch：`zhangchenjia21-dot/the-world` / `main`
- Task Base HEAD：`eab96a4`（2026-08-24 05:20 UTC，规划线程核验时 GitHub main HEAD）
- 工作区：`D:\AI\Projects\the world`

## 2. Outcome 与 Why Now

玩家（项目所有者）裁定：在正式判定系统建成前，临时启用接近 DND 的 d20 公开检定框架，保证"判定存在、判定公平、判定可见"。

实现已由规划线程在本地工作区落地并实测（文件清单见 §5），**尚未 commit / push**。本任务的唯一目标：验证现场完整性与一致性后，把这五个文件（且只这五个）提交并推送到 `main`，使判定包正式跨聊天生效。

## 3. Authority / Source Manifest

1. 用户当前明确指令（2026-08-24：启用 d20 过渡判定；骰子来源选定 tools/ 方案；保留已落地文件由执行线程提交）；
2. `docs/PRODUCT_SPEC_CURRENT.md` — the-world/main current；
3. `docs/GAME_WORKSPACE_ARCHITECTURE_v0.2.md` — the-world/main current；
4. `AGENTS.md`（the-world 仓库根）— 尤其 §11 Freshness & Writeback、§14 Public Repo Safety；
5. 本任务包。

不权威：历史聊天、旧摘要、模型记忆。

## 4. Read First（最小充分工作集）

1. `D:\AI\Projects\the world\AGENTS.md`
2. 本任务包
3. `library/mechanics/判定与检定_Expansion_Pack_v0.1.md`（新文件，通读一遍校验内容）
4. `tools/掷骰.mjs`（新文件）
5. `git status` / `git diff` 实际输出

## 5. DEC / INV / AC / NON

### DEC（已裁定，不得重开）

- DEC-1：判定层为**过渡包**，采用 d20 + 加值 vs DC（10/15/20/25/30 五档），优势/劣势单条简化规则；正式判定系统建成后本包退役归档；
- DEC-2：骰面唯一来源是 `tools/掷骰.mjs`（`node:crypto` 真随机），禁止模型自称随机；
- DEC-3：判定包为**每局 opt-in**，本次只给 `games/luan-shi-sanguo` 启用；`games/_template` 不设默认；
- DEC-4：判定包是裁定型机制，无长期状态，永不建 `STATE.md`。

### INV（不变量）

- INV-1：`git add` 只使用显式路径，禁止 `git add -A` / `git add .`；
- INV-2：游戏会话文件（`state/`、`story/`、`memory/`、`mechanics/traveler-system/`、`saves/SAVE-02_*` 等）**不是本任务资产**，一概不提交、不改动、不覆盖；
- INV-3：提交前必须重新核验 GitHub main HEAD（Pre-push Freshness Revalidation）；HEAD 已变则先审计增量再决定 rebase / 吸收；
- INV-4：public 仓库，不提交任何凭证 / 私密内容。

### AC（验收）

- AC-1：`node tools/掷骰.mjs 1d20+4`、`... adv`、`... dis`、`3d6` 均正常输出；非法输入非零退出；
- AC-2：`games/luan-shi-sanguo/COMPOSITION.md` 拓展包计数为 11，含判定与检定条目与 2026-08-24 确认记录；
- AC-3：commit 只包含且完整包含 §7 五个文件路径；
- AC-4：push 后 GitHub main 可见全部五个文件，远程 HEAD 更新，游戏会话未提交文件仍留在本地工作树。

### NON（禁止范围）

- 不修改 `plugins/`（World Core 提示文本不动；COMPOSITION 已随恢复注入对 GM 可见）；
- 不改判定包规则内容（如发现内容问题，停止并回报规划线程，不自行修订）；
- 不提交游戏会话文件；
- 不代游戏会话做任何维护 / 归并。

## 6. Allowed / Prohibited Scope

Allowed：读取全仓；执行 §9 验证命令；对 §7 五个显式路径 `git add` + commit + push。

Prohibited：除上述外的一切写操作；`git add -A`；force-push；改动或删除工作区中任何未提交的他人文件。

## 7. Deliverables（本任务涉及的全部文件路径）

```text
library/mechanics/判定与检定_Expansion_Pack_v0.1.md   （新增）
tools/掷骰.mjs                                        （新增）
tools/README.md                                       （修改：登记工具）
games/luan-shi-sanguo/COMPOSITION.md                  （修改：启用第 11 个拓展包）
games/luan-shi-sanguo/mechanics/README.md             （修改：登记裁定型机制）
```

## 8. Acceptance Gates

§5 AC-1 至 AC-4 全部通过；push 前 HEAD revalidation 通过。

## 9. Validation Commands

```bash
cd "D:/AI/Projects/the world"
node tools/掷骰.mjs 1d20+4
node tools/掷骰.mjs 1d20+4 adv
node tools/掷骰.mjs 1d20 dis
node tools/掷骰.mjs 3d6
node tools/掷骰.mjs abc ; echo "exit=$?"   # 期望非零
git status -s                               # 确认游戏会话文件与本任务文件清晰可分
git diff --stat -- tools/README.md games/luan-shi-sanguo/COMPOSITION.md games/luan-shi-sanguo/mechanics/README.md
```

提交命令（显式路径）：

```bash
git add "library/mechanics/判定与检定_Expansion_Pack_v0.1.md" \
        "tools/掷骰.mjs" "tools/README.md" \
        "games/luan-shi-sanguo/COMPOSITION.md" \
        "games/luan-shi-sanguo/mechanics/README.md"
git commit -m "feat(mechanics): 判定与检定过渡包 v0.1（d20 公开检定）+ tools/掷骰.mjs"
git push origin main
```

## 10. Git / Integration 责任

- 提交前：核验远程 HEAD 是否仍为 `eab96a4` 之后可预期的增量；若远程出现新 commit，先 `git fetch` 审计增量与本任务是否冲突；
- 提交时：仅 §7 五个显式路径；
- push 后：回报远程新 HEAD SHA。

## 11. Stop / Return Conditions

出现以下任一情况立即停止并回报，不自行处置：

- 远程 HEAD 增量触及本任务五个文件或 library/mechanics、tools 目录语义；
- `git status` 显示五个目标文件之外还有无法归因的改动；
- 判定包正文与 current 产品 / 架构文档出现实质冲突；
- 游戏会话正在写入目标文件（文件 mtime 极新且内容不稳定）。

## 12. Final Report 格式

```text
Task: TW-ASSET-2026-08-24-01
Base HEAD / Final remote HEAD: <sha> / <sha>
Validation: AC-1..AC-4 PASS/FAIL（各一行证据）
Commit: <sha> <message>
Files: 5/5 committed, 0 extra
Deviations: 无 / 描述
```
