---
title: 任务包｜WC-08 设计文档回迁与 DSH 克隆清理
status: current
version: 1.0
created: 2026-08-24
task_id: TW-DOC-2026-08-24-02
task_type: documentation-salvage + integration-commit + cleanup
scope: the-world-project
---

# 任务包｜WC-08 设计文档回迁与 DSH 克隆清理

## 1. 任务标识

- Task ID：`TW-DOC-2026-08-24-02`
- 类型：documentation-salvage（文档回迁）+ integration-commit + cleanup（删除废弃克隆）
- Owner：执行线程（由项目所有者指派）
- Repo / branch：`zhangchenjia21-dot/the-world` / `main`
- Task Base HEAD：`3381595`（2026-08-24 06:16 UTC，规划线程核验时 GitHub main HEAD）
- 正式工作树：`D:\AI\Projects\the world`（唯一工作树，见 DEC-4）
- 回迁来源（只读，禁止修改）：`D:\AI\deepseekharness\user-repos\the-world`

## 2. Outcome 与 Why Now

WC-08 Game Composition（新局组合确认：World / 角色 / 拓展包 / 操控模式，Required-Recommended-Optional 分级，`Enabled != Installed`，确认门）**已被实现**——插件确认门 `readCompositionStatus`、每局 `COMPOSITION.md`、v0.2 架构均已落地——但设计文档从未提交：GitHub main 的 `docs/TW-01_WORLD_CORE_PLAN.md` 完全没有 WC-08。设计的唯一文字记录是 DSH 克隆 `user-repos\the-world` 里 8 月 23 日晚的未提交改动。

本任务目标：

1. 把 WC-08 设计批次**适配回填** main（不是覆盖）；
2. 处置 `library/worlds/三国/` 草稿（独有内容并入，已覆盖部分放弃）；
3. push 验证后**删除废弃克隆**；
4. 消除文档与代码漂移，恢复"计划文档 = 实施真相"。

## 3. Authority / Source Manifest

1. 用户当前明确指令（2026-08-24：同意唯一工作树裁定；同意回迁后删克隆）；
2. 回迁源内容：`D:\AI\deepseekharness\user-repos\the-world` 工作树（= HEAD `47f1422` + 未提交改动）；
3. 回填基底：`D:\AI\Projects\the world` 当前工作树（应对齐 GitHub main `3381595`）；
4. `docs/GAME_WORKSPACE_ARCHITECTURE_v0.2.md` — current（已含 COMPOSITION.md 骨架地位）；
5. `AGENTS.md`（the-world 仓库根）；
6. 本任务包。

不权威：历史聊天、旧摘要、模型记忆。

## 4. Read First（最小充分工作集）

1. `D:\AI\Projects\the world\AGENTS.md`
2. 本任务包
3. 回迁源 diff（在 `user-repos\the-world` 执行 `git diff` 与 `git status -s`）+ 未跟踪目录 `library\worlds\三国\`（README.md、拓展清单.md）
4. 主树当前版：`docs/TW-01_WORLD_CORE_PLAN.md`、`docs/ARCHITECTURE_CURRENT.md`、`docs/PRODUCT_SPEC_CURRENT.md`、`AGENTS.md`、`plugins/README.md`、`games/_template/README.md`
5. 主树 `library/worlds/汉末三国_天下未定_World_Pack_v0.2.3.md`（对照三国草稿用）

## 5. DEC / INV / AC / NON

### DEC（已裁定，不得重开）

- DEC-1：回迁方式是**适配回填**——以主树当前文件为基底，把 WC-08 语义按 main 现行措辞并入；禁止用克隆版整文件覆盖（这些文件在 main 已经历 v0.2 时代演进）；
- DEC-2：`三国/` 草稿只做内容对照：世界口径选项、起始时代菜单、拓展清单分级等**独有内容**并入 `汉末三国_天下未定_World_Pack` 或合适 Owner（并入时提升世界包 minor 版本并在文内注明）；已被 v0.2.3 覆盖的部分不回迁；草稿目录本身不复制进主树；
- DEC-3：克隆删除只发生在 push 验证通过之后；
- DEC-4：the-world 唯一工作树 = `D:\AI\Projects\the world`；DSH 会话一律指向它，不再使用 `user-repos` 克隆（项目所有者 2026-08-24 批准）。

### INV（不变量）

- INV-1：对回迁源目录**只读**——不修改、不清理、不移动，直到 §9 删除步骤；
- INV-2：`git add` 只用显式路径，禁止 `git add -A`；
- INV-3：不触碰 `games/luan-shi-sanguo/` 下任何文件（游戏现场）；
- INV-4：回填是增量增补，若发现主树某处已有更新措辞表达同一语义，以主树为准，不制造重复段落；
- INV-5：public 仓库，不提交任何凭证 / 私密内容；
- INV-6：push 前重新核验远程 HEAD（Pre-push Freshness Revalidation）。

### AC（验收）

- AC-1：主树 `docs/TW-01_WORLD_CORE_PLAN.md` 含 WC-08 节（Game Composition），且 WC-07 恢复语义引用本局 Composition 配置；
- AC-2：`docs/ARCHITECTURE_CURRENT.md`、`docs/PRODUCT_SPEC_CURRENT.md`、`AGENTS.md`、`plugins/README.md`、`games/_template/README.md` 中 Game Composition / COMPOSITION.md 各有与现行措辞一致的登记（单处一两行，不重复铺陈）；
- AC-3：`三国/` 草稿处置有明确记录：独有内容去向（并入哪个文件）或放弃理由，写入 commit message 或 Final Report；
- AC-4：主树 `git diff` 审查确认无 v0.2 既有内容被误删；
- AC-5：push 后远程可见全部回填，远程 HEAD 更新；
- AC-6：AC-5 通过后，`D:\AI\deepseekharness\user-repos\the-world` 目录已删除。

### NON（禁止范围）

- 不修改插件代码（WC-08 已实施，本任务纯文档）；
- 不改动 `games/luan-shi-sanguo/`；
- 不改动 `library/worlds/埃瑟维亚_诸界余辉_World_Pack_v0.1.3.md`；
- 不重开 WC-08 设计本身（语义已定稿并已被实现验证）；

## 6. Allowed / Prohibited Scope

Allowed：读全部相关文件；编辑 §7 所列主树文件；显式路径 add / commit / push；push 验证后删除废弃克隆目录。

Prohibited：修改回迁源（删除步骤前）；`git add -A`；force-push；触碰游戏现场；改动任务包未列出的文件。

## 7. Deliverables（允许编辑的主树文件）

```text
docs/TW-01_WORLD_CORE_PLAN.md        （新增 WC-08 节 + WC-07 引用）
docs/ARCHITECTURE_CURRENT.md         （补 Composition 登记）
docs/PRODUCT_SPEC_CURRENT.md         （补 player-confirmed composition 条目）
AGENTS.md                            （补 Composition 原则一行）
plugins/README.md                    （补 Composition 确认门说明）
games/_template/README.md            （补 COMPOSITION.md 条目，若现行版未含）
library/worlds/汉末三国_天下未定_World_Pack_v0.2.3.md  （仅当三国草稿有独有内容时并入并升 minor 版本）
```

最终实际改动文件以执行线程审查为准（允许少于上述清单，须在 Final Report 说明每个文件的处置）。

## 8. Acceptance Gates

§5 AC-1 至 AC-6 全部通过。

## 9. Validation Commands

```bash
# 0. 现场检查
cd "D:/AI/Projects/the world" && git status -sb        # 确认基底与游戏现场文件可分
cd "D:/AI/deepseekharness/user-repos/the-world" && git status -s && git diff --stat

# 1. 回迁源内容提取（只读）
cd "D:/AI/deepseekharness/user-repos/the-world" && git diff

# 2. 回填后自检
cd "D:/AI/Projects/the world"
grep -n "WC-08" docs/TW-01_WORLD_CORE_PLAN.md
git diff --stat                                        # 审查无意外删除

# 3. 提交（显式路径，按实际改动调整）
git add docs/TW-01_WORLD_CORE_PLAN.md docs/ARCHITECTURE_CURRENT.md \
        docs/PRODUCT_SPEC_CURRENT.md AGENTS.md plugins/README.md \
        games/_template/README.md
git commit -m "docs: 回迁 WC-08 Game Composition 设计（消除文档与代码漂移）"
git push origin main

# 4. push 验证后，删除废弃克隆（本任务唯一破坏性步骤）
#    前置：确认远程已含回填 commit
rm -rf "D:/AI/deepseekharness/user-repos/the-world"
```

## 10. Git / Integration 责任

- 开始前：确认主树 `git fetch` 后远程 HEAD 与 Base `3381595` 的关系；远程若有新 commit，先审计增量是否触及 §7 文件；
- 提交：仅显式路径；
- push 后：记录远程新 HEAD SHA，然后才执行克隆删除。

## 11. Stop / Return Conditions

- 远程 HEAD 增量触及 §7 任一文件或 WC-08 相关语义；
- 主树文件当前内容与任务包描述严重不符（如 PLAN 已存在 WC-08）；
- 三国草稿与世界包 v0.2.3 出现无法安全判定的实质冲突；
- 主树 `git status` 出现无法归因的他人改动；
- 删除克隆前发现该目录在回填期间被任何会话再次写入。

出现以上任一情况：停止，保留现场，回报规划线程，不自行处置。

## 12. Final Report 格式

```text
Task: TW-DOC-2026-08-24-02
Base HEAD / Final remote HEAD: <sha> / <sha>
Validation: AC-1..AC-6 PASS/FAIL（各一行证据）
Commit: <sha> <message>
Files changed: <清单>（每个文件一行处置说明）
三国草稿处置: <并入何处 / 放弃理由>
Clone deleted: yes/no（路径）
Deviations: 无 / 描述
```
