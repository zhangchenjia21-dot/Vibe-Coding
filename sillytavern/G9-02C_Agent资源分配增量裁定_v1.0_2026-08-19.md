# G9-02C｜Agent 资源分配增量裁定 v1.0

状态：`CURRENT EXECUTION GOVERNANCE ADDENDUM`
日期：2026-08-19

本文件只更新当前 G9-02C 的具体资源安排；未修改 `G9及后续阶段_Agent资源分配与GrokBuild代码协作裁定_v1.0_2026-08-19.md` 的长期风险分工原则。

## 1. Current Decision

当前项目事实已经推进到：

```text
G9-02BC  PASS / CLOSED
G9-02B   PASS / CLOSED
G9-02C   ACTIVE / NEXT
```

当前仍约有一次 Sol 深任务可用。

因此正式采用：

```text
G9-02C Core
→ Sol

GPT
→ exact-SHA Independent Review

G9-02C Breadth
→ Grok Build

GPT
→ exact-SHA Independent Review / Stage Gate
```

## 2. 为什么 Core 用 Sol

02C Core 会冻结或修改：

- Model-first Routing responsibility；
- routing working-set hierarchy / pruning；
- Context Orchestrator internal contract；
- state-mandatory augmentation boundary；
- authorized action → domain orchestration evidence boundary；
- selected-only JIT projection anchors；
- outcome-gated continuation context activation。

这些全部命中长期治理的 Sol Reserved Work：Context Orchestrator、model-visible boundary、高耦合 internal contract、错误后会放大到 G9-03+。

## 3. Grok Build 后续范围

只有 Core exact-SHA Review PASS 后，Grok Build 才开放：

- concrete routing profile / hierarchy breadth；
- large registry stress；
- Player-known scale stress；
- large relation graph deterministic subgraph fixture；
- continuation branch matrix；
- background zero-model-call tests；
- integrated regression / cleanup。

若 Grok 发现必须重新定义：

- routing authority；
- player authorization；
- Formal Turn mutation cardinality；
- Save / Recovery；
- model-visible boundary；

必须返回 `BLOCKED / ARCHITECTURE QUESTION`。

## 4. Worktree

Core 和 Breadth 都继续遵守：

```text
main = protected integration line
agent/<task-id> = current temporary construction branch
D:\AI\Projects\.worktrees\sillytavern-agent = isolated worktree
```

Executor 完成后只 push task branch；GPT exact-SHA Review PASS 后才 fast-forward main。

## 5. Current Next

> `G9-02C_ModelFirstRouting与ContextOrchestration核心规格_v1.0_2026-08-19.md` 已冻结。下一步生成并执行 Sol Core 的 bounded worktree task packet。
