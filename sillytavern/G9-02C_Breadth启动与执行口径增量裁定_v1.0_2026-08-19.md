# G9-02C｜Breadth 启动与执行口径增量裁定 v1.0

状态：`CURRENT EXECUTION ADDENDUM / BREADTH ACTIVE`
日期：2026-08-19

本文件只收口 G9-02C Core PASS 后的执行状态，不改变 `G9-02C_ModelFirstRouting与ContextOrchestration核心规格_v1.0_2026-08-19.md` 的技术语义。

## 1. Current Gate

```text
G9-02C Core
PASS / CLOSED
Final SHA = 182740801b48c2edc2399e4e4dd8b6ae5a43ccaa

↓

G9-02C Breadth
ACTIVE / NEXT
Executor = Grok Build

↓

GPT exact-SHA Independent Review

↓

G9-02 Integrated Closure

↓

G9-03
```

G9-03 继续 `NOT AUTHORIZED`。

## 2. Authority clarification

若 `酒馆游戏新版主体重建总路线 v2.3.md` 的 Agent 资源段仍出现旧的：

```text
G9-02C high-risk core = RESERVED / NEXT
```

该行仅为未及时同步的旧执行状态，现由以下更新事实 supersede：

1. `G9-02C_IndependentReview_Core最终收口_v1.0_2026-08-19.md`；
2. 本增量裁定。

Roadmap 顶层关键路径中的 `G9-02C Context Orchestration breadth ACTIVE / NEXT` 保持有效。

## 3. Breadth scope

沿 frozen Core rails 扩：

- B1 Routing Breadth；
- B2 Projection / Scale Breadth；
- B3 Continuation / Background Breadth；
- B4 Integrated Regression / long-session / recovery / metrics / real Provider routing evidence。

不得重新设计 Core authority。

## 4. Stop Gate

若 Breadth 正确实现要求改变以下任一项，必须返回 GPT：

- routing authority；
- Player Agency / authorization；
- Formal Turn mutation cardinality；
- Save / Restore / Recovery authority；
- model-visible boundary；
- G9-03 external asset/resource schema。

## 5. Task Base

```text
Implementation repo
zhangchenjia21-dot/sillytavern

Task Base / current main
182740801b48c2edc2399e4e4dd8b6ae5a43ccaa
```

Breadth 必须新建独立临时分支，不复用已关闭的 Core branch。
