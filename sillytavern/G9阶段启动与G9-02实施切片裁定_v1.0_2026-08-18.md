# G9｜阶段启动与 G9-02 实施切片裁定 v1.0

状态：`CURRENT DECISION / G9 ACTIVE / G9-02B NEXT`
更新：2026-08-19

## 1. 当前阶段事实

```text
G1–G8                      PASS / CLOSED
G9-01 Compatibility Audit  PASS / CLOSED
G9-02A                     PASS / CLOSED
G9-02A final SHA            04603e1e4a3270e9f5740b5957cf545a2bd001d0
G9-02BC Shared Foundation   PASS / CLOSED
G9-02BC final SHA           5962e6f5933f245693e090cbdfd2f79791820ef1
G9-02BC Independent Review  PASS / CLOSED
G9                          ACTIVE
G9-02B breadth              ACTIVE / NEXT
G9-02C breadth              BLOCKED BY G9-02B breadth
G9-03                       NOT AUTHORIZED
```

G9-02A 已完成 Source Binding、Game-local Definition Revision、typed existing-asset mutation 与 Save / Restore / Branch / Recovery。

G9-02BC 已建立并证明 02B / 02C 共用的 Program-owned Domain Host、Package/Feature/Module activation、owner-scoped canonical/state extension、typed change/event/handoff、Routing Directory、selected-only JIT Projection 与 bounded owner-preserving context join。

---

## 2. G9-02 内部实施结构

```text
G9-02A Source Binding + Game-local Revision Foundation    PASS / CLOSED
↓
G9-02BC Shared Runtime Foundation Convergence             PASS / CLOSED
↓
G9-02B Runtime Domain / Expansion Binding Host breadth    ACTIVE / NEXT
↓
G9-02C Context Orchestration breadth
↓
G9-02 Integrated Closure
↓
G9-03 asset-spec vNext
```

`G9-02BC` 是 02B / 02C 的 cross-slice shared foundation，不是新增生命周期阶段。

---

## 3. G9-02A｜PASS / CLOSED

正式对象：

- Implementation：`sillytavern@04603e1e4a3270e9f5740b5957cf545a2bd001d0`
- Spec：`G9-02A_SourceBinding与GameLocalRevisionFoundation规格_v1.0_2026-08-18.md`
- Review：`G9-02A_IndependentReview_SourceBinding与GameLocalRevision_v1.0_2026-08-19.md`

已证明 source per-game binding / lineage、typed Game-local mutation、revision、Save / Restore / Branch / Recovery exactly-once。

---

## 4. G9-02BC｜PASS / CLOSED

正式对象：

- Implementation：`sillytavern@5962e6f5933f245693e090cbdfd2f79791820ef1`
- Spec：`G9-02BC_SharedRuntimeFoundationConvergence规格_v1.0_2026-08-19.md`
- Review：`G9-02BC_IndependentReview_共享运行时基础_v1.0_2026-08-19.md`

已证明：

- Program-built module/owner registry；
- Asset data-only / no arbitrary code；
- Package Included != Feature Enabled != Module Enabled；
- hard dependency availability != Context inclusion；
- Domain Canonical Record 复用 G9-02A metadata；
- Domain Runtime State 独立持久化；
- Domain change 与 Formal Turn 原子提交；
- Event/Handoff owner identity；
- disabled module fail-closed；
- Routing selection 后才做 JIT Projection；
- 100-module fixture 只投影 selected 1；
- Save / Restore / Branch / Recovery；
- migration rollback。

Shared Foundation 没有完成完整 Model-first Router，也没有冻结 external schema。

---

## 5. G9-02B breadth｜ACTIVE / NEXT

默认执行者：Grok Build。

目标：

- package / feature / module registry breadth；
- concrete built-in domain modules against frozen Host；
- owner-typed extension canonical record/state breadth；
- definition registry / G8 UI Host definition/contribution binding；
- **Player-known Entity / Character Directory**；
- persistence / migration / regression breadth。

正式区分：

```text
Canonical Character Truth
!= Player-known Character Directory
!= Current Scene Visible Character Set
```

People Surface 面向长期 player-known / last-known safe records：

- 玩家合法认识 / 确认身份后进入目录；
- 角色离场、远行、失踪、死亡不因“不在当前场景”自动删除；
- 未认识的 Game-local Character 不得泄露；
- current presence 仍是独立的即时互动集合；
- 不提前冻结 People 分类 taxonomy。

02B 不实现完整 Model-first Router。

---

## 6. G9-02C breadth｜BLOCKED BY 02B

在 02B PASS 后开始。

```text
Player Input
+ enabled/pruned Routing Directory
+ minimal current context
→ Model-first routing
→ Program structural validation + state-mandatory augmentation
→ JIT owner projections
→ bounded owner-preserving join
→ domain handling / Formal Outcome
→ outcome-gated continuation
```

必须证明：

- Semantic AI judges open routing semantics；Program 不用关键词重复 NLP；
- evidence / agency / Open Attempt preserved；
- disabled module fail-closed；
- deterministic background zero-model-call；
- no transitive prompt expansion；
- Bounded != Starved；
- large enabled module set 有进一步 routing working-set pruning；
- G8 Materialization Need 不回滚；
- Known Character Directory 增长不导致 ordinary Turn context 线性增长。

---

## 7. 资源安排

```text
G9-02B breadth          Grok Build  ACTIVE / NEXT
GPT                     exact-SHA Independent Review
最后约一次 Sol 深任务    RESERVED for G9-02C core
其余 02C breadth        Grok Build
```

最后一次 Sol 暂不消耗在 02B 常规 breadth。02B 是 Grok Build 正式接入后端代码的首个主任务；若其 PASS，Sol 优先用于 02C 的模型路由 / 上下文编排高风险核心。

若套餐变化使剩余 Sol 不可用，02C 仍可由 Grok Build 按 02BC frozen rails 实现，不形成 blocker。

---

## 8. Long-lived Product Surface Scalability

长期资料 Surface：

```text
People
Information / Knowledge
Objective / Task
future long-lived dossier surfaces
```

G9 保证正确 owner、stable identity、player-safe metadata seam；具体 grouping / filter / sort / search / history / pagination 属于 G11 Product maturity。

Items 继续区分 inventory/current scene visibility，不自动变成历史物品目录。

---

## 9. G9-03 Freeze Gate

只有 02B breadth + 02C breadth + Integrated Closure 后，才能冻结 machine asset identity、source lineage、dependency/feature/module wire、context contract、mutable-field policy、runtime binding、UI declaration、manifest/bundle relationship 与 compatibility/migration metadata。

额外禁止：

- 把 Player-known Character Directory 建成 Source Character Card 字段；
- 把 current scene visibility 与 long-lived acquaintance/knowledge lifecycle 合并；
- 提前冻结 People / Objective / Information UI taxonomy；
- 把 Discussion-only Opening Scenario 直接写进 external schema。

---

## 10. 当前 Next

> **G9-02B｜Runtime Domain / Expansion Binding Host breadth + Player-known Entity Directory。**

Implementation Base：`5962e6f5933f245693e090cbdfd2f79791820ef1`。

默认执行者：Grok Build。

G9-03 当前 `NOT AUTHORIZED`。
