# G9｜阶段启动与 G9-02 实施切片裁定 v1.0

状态：`CURRENT DECISION / G9 ACTIVE / G9-02BC NEXT`
更新：2026-08-19

## 1. 当前阶段事实

```text
G1–G8                      PASS / CLOSED
G9-01 Compatibility Audit  PASS / CLOSED
G9-02A                     PASS / CLOSED
G9-02A final SHA            04603e1e4a3270e9f5740b5957cf545a2bd001d0
G9-02A Independent Review   PASS / CLOSED
G9                          ACTIVE
G9-02BC Shared Foundation   ACTIVE / NEXT
G9-02B breadth              BLOCKED BY G9-02BC
G9-02C breadth              BLOCKED BY G9-02BC + G9-02B breadth
G9-03                       NOT AUTHORIZED
```

G9-01 结论继续成立：现有 World Pack / Character Card / Expansion 语义架构总体兼容；真正需要先补齐的是 Runtime 资产承接、Domain Host 与 Context Orchestration foundation。

G9-02A 已完成 Source Binding、Game-local Definition Revision、typed existing-asset mutation 与 Save / Restore / Branch / Recovery，并通过 exact-SHA Independent Review。

---

## 2. G9-02 内部实施结构

原 G9-02 同时覆盖 persistence、Game-local mutation、Expansion Host、Domain extensibility、Context Router，blast radius 过大。

当前实施结构调整为：

```text
G9-02A Source Binding + Game-local Revision Foundation    PASS / CLOSED
↓
G9-02BC Shared Runtime Foundation Convergence             ACTIVE / NEXT
↓
G9-02B Runtime Domain / Expansion Binding Host breadth
↓
G9-02C Context Orchestration breadth
↓
G9-02 Integrated Closure
↓
G9-03 asset-spec vNext
```

`G9-02BC` 是 02B / 02C 的 cross-slice shared foundation，**不是新增生命周期阶段**。其目的只是先冻结两条 breadth 共同依赖的高耦合 rails，再让后续 Grok Build 按冻结合同扩展。

---

## 3. G9-02A｜PASS / CLOSED

正式对象：

- Implementation：`sillytavern@04603e1e4a3270e9f5740b5957cf545a2bd001d0`
- Spec：`G9-02A_SourceBinding与GameLocalRevisionFoundation规格_v1.0_2026-08-18.md`
- Review：`G9-02A_IndependentReview_SourceBinding与GameLocalRevision_v1.0_2026-08-19.md`

已证明：

- Source Asset identity/version/hash 按值绑定到单局；
- 每局拥有独立 Game-local identity / lineage；
- Source 后续变化不静默进入旧局；
- Item / Character / Scene existing Game-local definition 可 typed patch；
- Program 拥有 mutation validation / commit；
- local definition revision 可 Save / Restore / Branch；
- Crash / Recovery 不重复 Semantic / mutation authoring；
- Source 不被反写；
- future ownerNamespace / assetKind common metadata seam 不被五类 Core 锁死。

真实 reference case“重要信件”已完成。

---

## 4. G9-02BC｜Shared Runtime Foundation Convergence

正式规格：`G9-02BC_SharedRuntimeFoundationConvergence规格_v1.0_2026-08-19.md`。

当前只建立：

```text
Built-in Domain Module Registry / Host
↓
Package / Feature / Module Binding + Activation
↓
owner-scoped Canonical Record / Runtime State seam
↓
typed Candidate / Change / Event / Handoff / Projection seam
↓
Routing Directory boundary
↓
validated selection
↓
JIT Context Projection Host
↓
bounded owner-preserving join boundary
```

要求 one reference vertical、disabled fail-closed、Save/Restore/Branch/Recovery 与 large-registry bounded projection proof。

明确 NON-SCOPE：

- 所有真实 Expansion breadth；
- 完整 People Directory；
- 完整 Model-first Router；
- external asset-spec；
- Adapter / Compiler；
- Creator；
- Objective Engine；
- Prologue Runtime。

---

## 5. G9-02B breadth

Shared Foundation Review PASS 后开始，默认由 Grok Build 主导。

目标：

- package / feature / module registry breadth；
- concrete built-in domain modules against frozen Host；
- owner-typed extension canonical record/state breadth；
- UI Host definition/contribution binding；
- Player-known Entity / Character Directory；
- persistence / migration / regression breadth。

正式区分：

```text
Current Scene Visible Characters
!= Player-known Character Directory
```

People Surface 面向长期 player-known safe records；角色离场不删除，未认识 NPC 不泄露。

---

## 6. G9-02C breadth

在 Shared Foundation + 02B breadth 后开始，默认由 Grok Build 在 GPT 严格 Spec 下实现。

```text
Player Input
+ enabled/pruned Routing Directory
+ minimal current context
→ Model-first routing
→ Program structural validation + state-mandatory augmentation
→ JIT owner projections
→ bounded owner-preserving join
→ domain handling
→ outcome-gated continuation
```

必须证明：

- disabled module fail-closed；
- deterministic background zero-model-call；
- large registry selected projection；
- no transitive prompt expansion；
- Bounded != Starved；
- Open Attempt；
- G8 Materialization Need 不回滚；
- Known Character Directory 增长不导致 ordinary Turn context 线性增长。

---

## 7. Long-lived Product Surface Scalability

长期资料 Surface：

```text
People
Information / Knowledge
Objective / Task
future long-lived dossier surfaces
```

G9 保证正确 owner、stable identity、player-safe metadata seam；具体 grouping / filter / sort / search / history / pagination 属于 G11 Product maturity。

Items 继续区分 inventory/current scene visibility，不自动变成历史物品目录。

---

## 8. G9-03 Freeze Gate

只有 G9-02 Shared Foundation + 02B breadth + 02C breadth + Integrated Closure 后，才能冻结：

- machine asset identity；
- source lineage；
- dependency / feature / module wire；
- context contract wire；
- mutable-field policy；
- runtime binding metadata；
- UI declaration wire；
- manifest / bundle relationship；
- compatibility / migration metadata。

额外禁止：

- 把 Player-known Character Directory 建成 Source Character Card 字段；
- 把 current scene visibility 与 long-lived acquaintance/knowledge lifecycle 合并；
- 提前冻结 People / Objective / Information UI taxonomy；
- 把 Discussion-only Opening Scenario 直接写进 current external schema。

---

## 9. Agent 执行策略

当前资源分工：

```text
G9-02A                    Codex Sol     PASS / CLOSED
G9-02BC Shared Foundation 最后一次 Sol  ACTIVE / NEXT
G9-02B breadth            Grok Build
G9-02C breadth            Grok Build
G9-02 Integrated Closure  Grok Build + GPT Review
G9-03 semantics           GPT
G9-03 implementation      Grok Build
G9-04 Adapter / Compiler  Grok Build
G9-05 Creator             Grok Build
```

正式治理：`G9及后续阶段_Agent资源分配与GrokBuild代码协作裁定_v1.0_2026-08-19.md`。

---

## 10. 当前 Next

> **G9-02BC｜Shared Runtime Foundation Convergence。**

Implementation Base：`04603e1e4a3270e9f5740b5957cf545a2bd001d0`。

G9-03 当前 `NOT AUTHORIZED`。
