# 酒馆游戏新版主体重建总路线 v2.0

状态：`CURRENT ROADMAP`
日期：2026-08-19
version_policy: human-decimal-one-digit-minor

## 0. 总路线

```text
G1 真实模型兼容性                 PASS / CLOSED
G2 最小世界运行内核               PASS / CLOSED
G3 正式世界能力垂直扩展           PASS / CLOSED
G4 程序判定与 Dice                PASS / CLOSED
G5 长期世界连续性                 PASS / CLOSED
G6 Save / Continue / Restore      PASS / CLOSED
G7 Crash / Resume / Recovery      PASS / CLOSED
G8 网页产品化                     PASS / CLOSED
G9 资产生态接入                   ACTIVE / G9-02BC Shared Foundation
G10 Provider 扩展                 FUTURE
G11 Alpha / 全系统验收            FUTURE
G12 Windows Release               FUTURE
```

## 1. G8 最终收口

Final code：`cdbd9cd7ff0b5b9a5672156066478b57f732307c`

```text
G8-UAT-01           PASS / CLOSED
G8-UAT-02           PASS / CLOSED
Independent Review  PASS
Project Owner UAT   PASS WITH NON-BLOCKING UX FINDINGS
P0                  0
P1                  0
G8                  PASS / CLOSED
```

Owner UAT 非阻塞体验项：`G8_StageUAT_最终收口与体验Backlog_v1.0_2026-08-18.md`。

People Surface 正式语义：`17_酒馆游戏_PlayerKnownEntityDirectory与长期资料表面信息架构裁定_v1.0_2026-08-18.md`。

---

## 2. G9-01｜Compatibility Audit

状态：`PASS / CLOSED`。

正式审计：`G9-01_资产兼容性审计与G9-02基础门禁_v1.0_2026-08-18.md`。

```text
Semantic Asset Architecture       COMPATIBLE
Existing Assets Rewrite           NOT REQUIRED
Current Runtime Asset Foundation  PARTIAL
G9-02 Foundation                  REQUIRED
G9-03 Schema Freeze               BLOCKED BY G9-02
```

现有 World Pack / Character Card / Expansion 的 Canonical Ownership、Definition/Instance、Program Authority、typed dependency/handoff、Feature/Module activation、Runtime Context Contract 与 UI ownership 语义总体兼容。

---

## 3. 当前关键路径

```text
G9-01 Compatibility Audit                       PASS / CLOSED
↓
G9-02A Source Binding + Game-local Revision      PASS / CLOSED
↓
G9-02BC Shared Runtime Foundation Convergence    ACTIVE / NEXT
↓
G9-02B Runtime Domain / Expansion Host breadth
       + Player-known Entity Directory
↓
G9-02C Context Orchestration breadth
↓
G9-02 Integrated Closure
↓
G9-03 asset-spec vNext Machine Contract
↓
G9-04 Game Asset Adapter / Compiler
↓
G9-05 Creator rebuild
```

`G9-02BC` 是 02B / 02C 共同依赖的 cross-slice shared foundation，不新增生命周期阶段。

---

## 4. G9-02A｜PASS / CLOSED

Final code：`04603e1e4a3270e9f5740b5957cf545a2bd001d0`

Independent Review：`PASS / CLOSED`。

Review：`G9-02A_IndependentReview_SourceBinding与GameLocalRevision_v1.0_2026-08-19.md`。

已证明：

```text
Source Asset descriptor/version/hash
→ per-game bind + local lineage
→ source unchanged

existing Game-local asset
→ typed definition patch
→ Program validation / local revision
→ Product canonical projection
→ Save / Restore / Branch / Recovery
```

重要信件 reference case、two-game isolation、source immutability、typed Item/Character/Scene mutation、Save/Restore/Branch、Recovery exactly-once、hidden disclosure 与 ordinary zero mutation call 均完成。

---

## 5. G9-02BC｜Shared Runtime Foundation Convergence

当前 `ACTIVE / NEXT`。

规格：`G9-02BC_SharedRuntimeFoundationConvergence规格_v1.0_2026-08-19.md`。

只建立两条 breadth 共同依赖的 rails：

```text
Built-in Domain Module Host
↓
Package / Feature / Module Binding + Activation
↓
owner-scoped Canonical Record / Runtime State seam
↓
typed Candidate / Change / Event / Handoff / Projection
↓
Routing Directory boundary
↓
validated selection
↓
JIT Context Projection
↓
bounded owner-preserving join
```

要求 one production reference vertical + disabled fail-closed + persistence/recovery + large-registry bounded projection proof。

本轮不扩完所有 Domain、不做完整 Model Router、不冻结 asset-spec、不做 Creator / Adapter / Prologue Runtime。

---

## 6. G9-02B｜Runtime Domain / Expansion Host breadth

在 Shared Foundation Review PASS 后，由 Grok Build 默认主导。

目标：
- registry / activation breadth；
- concrete built-in domain modules against frozen Host；
- definition registry / G8 UI Host binding；
- Player-known Entity / Character Directory；
- persistence / migration / test breadth。

正式保持：

```text
Current Scene Visible Characters
!= Player-known Character Directory
```

People Surface 是长期 player-known safe directory；未认识 NPC 不泄露，角色离场不消失。

---

## 7. G9-02C｜Context Orchestration breadth

在 Shared Foundation + 02B breadth 后，由 Grok Build 在严格 Spec 下主导。

目标链：

```text
Player Input
+ enabled/pruned Routing Directory
+ minimal current context
→ Model-first routing
→ Program structural validation + state-mandatory augmentation
→ JIT owner projections
→ bounded owner-preserving join
→ domain handling / Formal Outcome
→ outcome-gated downstream continuation
```

必须验证：disabled fail-closed、background deterministic zero-model-call、large registry selected projection、no transitive prompt expansion、Bounded != Starved、Open Attempt。

People stress gate：

```text
Known Character Directory size ↑↑↑
ordinary unrelated Turn context ≈ bounded
```

---

## 8. G9-03 Freeze Gate

只有 Shared Foundation、02B breadth、02C breadth 与 G9-02 Integrated Closure 全部 PASS 后才能冻结：

- source asset identity/version/hash；
- source → local binding wire；
- immutable/evolvable/private field policy；
- owner / namespace；
- dependency / conditional / handoff / contribution；
- package / feature / module activation；
- routing / context contract；
- UI declaration；
- materialization / mutation eligibility；
- runtime binding metadata；
- compatibility / migration metadata；
- Manifest / Bundle relationship。

禁止根据 Markdown frontmatter 直接猜 final schema；禁止把 Player-known Directory 做成 Source Character Card 字段；禁止提前冻结长期资料 Surface UI taxonomy。

Opening Scenario 当前仍是 Discussion Draft，未经过 Creator authoring pilot 与 Runtime vertical，不进入当前 G9-03 freeze commitment。

---

## 9. G9-04｜Adapter / Compiler

G9-03 冻结后由 Grok Build 主导 deterministic implementation：

```text
World Pack / Character Card / Expansion source
→ parser / adapter / compiler
→ asset-spec vNext
→ Game-local binding
```

GPT 负责 compatibility / authority review。

---

## 10. G9-05｜Creator rebuild

Grok Build 主导 UI、form、preview、validation、reference/dependency selection、import/export；GPT 负责产品流程和 Creator UAT。

Opening Scenario 暂只规划最小 authoring prototype：先完成一份真实创作并复盘，再冻结 Creator / Runtime / schema。

---

## 11. Agent 资源路线

```text
G9-02A                        Codex Sol        PASS / CLOSED
G9-02BC Shared Foundation     最后一次 Sol     ACTIVE / NEXT
02B / 02C breadth             Grok Build
G9-02 Integrated Closure      Grok Build + GPT Review
G9-03 schema semantics        GPT
G9-03 implementation          Grok Build
G9-04 Adapter / Compiler      Grok Build
G9-05 Creator                 Grok Build
```

正式治理：`G9及后续阶段_Agent资源分配与GrokBuild代码协作裁定_v1.0_2026-08-19.md`。

---

## 12. Deferred Product / Provider Backlog

```text
G10 Provider Expansion
→ selectable supported DeepSeek model / Provider options

G11 Alpha
→ Game Library delete lifecycle
→ carried Item description display polish
→ full Objective / Task product vertical（若此前未完成）
→ People / Information / Objective grouping / filter / sort / search / history organization
```

---

## 13. 长期事实模型

```text
Source Asset Library
↓ snapshot / bind
Game-local Canonical Assets
↕ typed Program-owned mutation
Authoritative Runtime State
```

```text
Canonical Character Truth
!= Player-known Character Directory
!= Current Scene Visible Character Set
```

```text
Semantic AI judges open semantics
Program validates / commits reality
World Growth != Prompt Growth
Runtime Relevant != Model Visible
Player-known != Model Visible
```

---

## 14. 当前 Next

> **G9-02BC Shared Runtime Foundation Convergence implementation。**

Implementation Base：`04603e1e4a3270e9f5740b5957cf545a2bd001d0`。

G9-03 当前 `NOT AUTHORIZED`。
