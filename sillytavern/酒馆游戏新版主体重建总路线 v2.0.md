# 酒馆游戏新版主体重建总路线 v2.0

状态：`CURRENT ROADMAP`
日期：2026-08-18
version_policy: human-decimal-one-digit-minor

## 0. 总路线

```text
G1 真实模型兼容性                 PASS / CLOSED
G2 最小世界运行内核               PASS / CLOSED
G3 正式世界能力垂直扩展           PASS / CLOSED
G4 程序判定与 Dice                PASS / CLOSED
G5 长期世界连续性                 PASS / CLOSED
G6 Save / Continue / Restore      PASS / CLOSED
G7 Crash / Resume / Recovery      PASS / CLOSED
G8 网页产品化                     PASS / CLOSED
G9 资产生态接入                   ACTIVE / G9-01
G10 Provider 扩展                 FUTURE
G11 Alpha / 全系统验收            FUTURE
G12 Windows Release               FUTURE
```

## 1. G8 最终收口

Final code：

`cdbd9cd7ff0b5b9a5672156066478b57f732307c`

```text
G8-UAT-01              PASS / CLOSED
G8-UAT-02              PASS / CLOSED
Independent Review     PASS
Project Owner UAT      PASS WITH NON-BLOCKING UX FINDINGS
P0                      0
P1                      0
G8                      PASS / CLOSED
```

Owner UAT 非阻塞体验项已记录：

`G8_StageUAT_最终收口与体验Backlog_v1.0_2026-08-18.md`

---

## 2. 当前关键路径

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding + Context Orchestration + full Game-local Layer
↓
G9-03 asset-spec vNext
↓
G9-04 Adapter / Compiler
↓
G9-05 Creator rebuild
```

当前只授权 G9-01；后续 Gate 依赖上游证据。

---

## 3. G9-01｜Compatibility Audit

原有检查继续：

- Source / Game-local / Runtime State assumptions；
- real Creation / local instance path；
- capability provenance；
- affordance consistency；
- world growth / long-session semantics；
- bounded but sufficient context；
- integration of meaning；
- protocol freeze readiness。

新增本次 Owner UAT 输入：

1. Existing Game-local Item / Character / Place 的 evolvable definition fields 必须有 ownership / persistence / visibility compatibility；
2. `Item known description` / discovered public description patch 必须可由 G9-02 typed local mutation 正确表达；
3. Objective / Task 等未来长期记录不能被 asset-spec / Game-local boundary 提前封死；
4. Product Surface 只消费 canonical projection，不成为 mutation owner。

---

## 4. G9-02｜Runtime Asset Binding + Context Orchestration + Game-local Layer

必须继续证明：

```text
Source Asset
→ game-local bind / provenance
→ local mutation
→ Runtime binding
→ bounded Context projection
→ Save / Restore
→ source unchanged
```

新增真实 Reference Case：

```text
existing Item
→ player inspection reveals durable public/known detail
→ Model typed Item field patch
→ Program validation / commit
→ Product dossier refresh
→ Save / Restore same value
→ Source unchanged
```

这属于第 16 号裁定已冻结的 `Item known description` evolvable field，不是 G8 返修。

---

## 5. Deferred Product / Provider Backlog

```text
G10 Provider Expansion
→ selectable supported DeepSeek model / Provider options

G11 Alpha
→ Game Library delete lifecycle
→ carried Item existing-description display polish
→ full Objective / Task product vertical（若此前未完成）
```

Objective semantic compatibility 在 G9-01 检查，但完整 Objective Engine 默认不作为 G9-02 exit gate。

---

## 6. 长期世界事实模型

继续冻结：

```text
Source Asset Library
↓ snapshot / bind
Game-local Canonical Assets
↕ typed Program-owned mutation
Authoritative Runtime State
```

以及：

```text
Semantic AI judges open semantics
Program validates / commits reality
World Growth != Prompt Growth
Runtime Relevant != Model Visible
```

---

## 7. 当前 Next

> **G9-01 Compatibility Audit。**
