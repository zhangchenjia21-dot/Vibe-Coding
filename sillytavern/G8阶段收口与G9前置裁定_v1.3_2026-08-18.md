# G8｜阶段收口与 G9 前置裁定 v1.3

状态：`CURRENT DECISION`
日期：2026-08-18
supersedes: `G8阶段收口与G9前置裁定_v1.2_2026-08-18.md`

> v1.3 记录 WEB-04 Final Host Convergence 已正式 PASS，并执行第 15 号 Runtime Context Orchestration 裁定的 Decision Propagation。

## 1. 当前阶段事实

- G1–G7：PASS / CLOSED。
- G8-WEB-01～03：PASS / CLOSED / absorbed。
- G8-WEB-04 Final Host Convergence：**PASS / CLOSED**。
- G8-WEB-05 New Game / API / Creation 玩家主路径：PASS；当前只剩 Technical Migration Closure。
- G9：仍未开始，blocked by G8 Exit Gate。

WEB-04 正式实现：

`zhangchenjia21-dot/sillytavern@5c76f4302152a7598b54d7f9d5774616b1fd618d`

## 2. G8 前置重分类继续有效

### REQUIRED

1. WEB-05 Technical Migration Closure；
2. WEB-08 Controlled Multi-action Core Slice；
3. G8 Exit Gate。

### DEFERRED / NON-BLOCKING FOR G9

- WEB-06 Save Center / Recovery Product UX；
- WEB-07 Undo / Re-input Product UX；
- Creation Preview；
- 完整 Creation Semantic Review；
- 完整 Gameplay Recommendation；
- 深度 mobile / WCAG；
- 高级 Timeline / Archive / multi-device Recovery；
- 小型 UI polish。

这些在 Alpha / G11 前重新拉回 Required，不解释为永久取消。

## 3. 当前 G8 最短关键路径

```text
WEB-04 Final Host Convergence        ✓ PASS / CLOSED
↓
WEB-05 Technical Migration Closure   ← NEXT
↓
WEB-08 Controlled Multi-action Core Slice
↓
G8 Exit Gate
↓
G8 PASS / CLOSED
```

## 4. WEB-05 Technical Migration Closure 的 Exit 条件

进入 WEB-08 前必须做到：

```text
Formal Product New Game Authority
=
Creation Project only
```

退休 production：

- `GameCreationDraft`；
- One Draft / Five Sections mutation / generation / create path；
- ProductFoundationClient legacy Draft methods；
- `/api/product/new-game/drafts...` routes；
- `GameCreationPublicApi` legacy Draft store / generation / creation；
- obsolete Draft-only tests / adapters。

允许保留 `use_my_assets` / asset-flow 的 G9 product concept，但不得因此保留旧 Draft architecture。

## 5. WEB-04 PASS 后的 G9 Host 前置已满足

G9 可以依赖以下真实 Host facts：

- typed Runtime UI Definition；
- Core five / Extension Surface；
- nine Host Capabilities；
- Owner / Contributor / dependency / conflict；
- secondary View；
- safe component vocabulary；
- player-safe live contribution materialization；
- no arbitrary state path / expression / code execution；
- Creation Contribution current contract；
- sourceDefinitionId / ownerGameplayId；
- durable UI Preference / Restore separation。

但 G9 仍不得在 G8 CLOSED 前开始 formal machine schema implementation。

## 6. Decision Propagation：G9 Task DAG 已改变

第 15 号核心裁定已经要求 Runtime Context Orchestration 成为资产生态正式能力。

因此 G9 不再是：

```text
Compatibility Audit
→ asset-spec vNext
```

而是：

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding + Context Orchestration Foundation
↓
G9-03 asset-spec vNext Machine Contract
↓
G9-04 Game Asset Adapter / Compiler
↓
G9-05 Creator rebuild
```

G9-02 必须先用 internal handwritten runtime profiles 证明：

```text
Player Input
+
Enabled Expansion / Capability Directory
+
minimal current scene summary
↓
Router Model
↓
Immediate relevant Expansion / Domain / Intent candidates
↓
Program structural validation
+
state-mandatory augmentation
↓
Current Relevant Set
↓
JIT bounded Runtime Projection
```

原则：

> **Runtime Context Orchestrator Before Asset Context Schema。**

这条变化属于 P1 Task-DAG change：不打断已完成的 G8 Host 工作，但必须在 G9 schema freeze 前生效。

## 7. 当前 Next

唯一正式下一项：

> **G8-WEB-05｜Technical Migration Closure**

尚不开始 G9 Router / Context Orchestration / asset-spec / Creator 实现。
