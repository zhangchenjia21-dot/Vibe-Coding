# G8｜阶段收口与 G9 前置裁定 v1.8

状态：`CURRENT DECISION / UAT FIX`
日期：2026-08-18

## 1. 当前阶段事实

- G1–G7：PASS / CLOSED。
- G8-WEB-04 Host：PASS / CLOSED。
- G8-WEB-05 Creation Migration：PASS / CLOSED。
- G8-WEB-08 Controlled Multi-action：PASS / CLOSED。
- G8-UAT-01：PASS / CLOSED at `sillytavern@52d0421bc58449ac8763681816bc7a84de93b385`。
- Project Owner Stage UAT rerun：**FAIL / BLOCKED**。
- G8：**REOPENED / UAT FIX**。
- G9：**NOT AUTHORIZED**。

当前 blocker：

> `G8-UAT-02｜Game-local Semantic Materialization + Living World Convergence`

正式规格：

`G8-UAT-02_GameLocalSemanticMaterialization与活世界收口规格_v1.0_2026-08-18.md`

---

## 2. Stage UAT 新证据

当前 Creation compiler 为满足 Minimum T0，使用 deterministic generic entity：

- `附近的人`
- `附近区域`
- `随身物品`

真实 UAT 证明这些不是可接受的正常 player-facing world content：

- 多类 opening characters 被合并成一个人格混杂 NPC；
- 多个随身资源被压成一个 Item；
- 相邻 destination 只是 system placeholder；
- 世界缺少主动 opening beat 和后续 bounded world growth；
- Information Surface 混入 `ACTION_COMPLETED / PLAYER_MOVED`；
- Player Profile 没有完整消费 Creation player-safe fields。

因此：

> Minimum Count Gate 已通过，但 Semantic Fidelity / Living World Gate 未通过。

---

## 3. G8-UAT-02 Required

必须完成：

1. Creation Field Semantic Audit；
2. internal typed Materialization Plan；
3. configured AI-assisted Creation 的 bounded semantic materialization；
4. concrete opening NPC / Place / Item；
5. minimal Game-local canonical seed；
6. proactive opening beat；
7. plausible unknown local NPC / Place JIT materialization；
8. Inventory cardinality / Product consistency；
9. Information Surface semantic correction；
10. Player Profile complete projection；
11. Save / Restore stable identity proof；
12. Real DeepSeek Gate + Project Owner UAT。

---

## 4. 对 Final Create zero-call 的修正

旧规则不再解释为：

> 所有 Creation free text 都必须在 zero-model-call 下转换为结构化世界。

新规则：

### Manual / No-key

继续允许 deterministic creation，但不得用 generic placeholder 假装理解歧义自由文本。

### AI-assisted + configured Provider

允许 / 要求 bounded typed semantic materialization call：

```text
Creation semantic fields
→ strict materialization proposal
→ Program validation
→ atomic Game-local seed commit
```

这属于 AI-assisted Creation 的正式组成。

---

## 5. 第 16 号裁定提前形成最小纵切

完整 G9 Asset Ecosystem 仍不提前。

但 G8 Exit 前必须 production-proof：

```text
Creation semantic brief
→ Game-local concrete assets
→ Runtime binding
→ proactive opening beat
→ bounded local world materialization
→ stable identity
→ authoritative commit
→ Narrative / Product consistency
```

这不是 general procedural world generation。

原则继续是：

> **Models may author and evolve game-local reality, but only through Program-owned typed commit boundaries.**

---

## 6. G8-UAT-01 成果继续有效

不得回滚：

- Program Final Outcome authority；
- No Phantom Interactable；
- location / interactable authority；
- Bounded Rich Context；
- Ephemeral Dialogue Freedom；
- Player Agency；
- exactly-five grounded suggestions；
- Save / Restore / Recovery / multi-action。

新的动态实体必须先成为 authoritative Game-local / Runtime referent，Narrative 才可呈现。

---

## 7. 当前关键路径

```text
G8-UAT-01                 PASS / CLOSED
Stage UAT rerun           FAIL
↓
G8-UAT-02                 ACTIVE / REQUIRED
↓
Independent Review
↓
Project Owner Stage UAT
↓
G8 PASS / CLOSED
↓
G9-01 Compatibility Audit
```

---

## 8. G9 Task DAG

顺序不变：

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding
      + Context Orchestration
      + full Game-local Canonical Asset Layer
      + Runtime World Materialization Foundation
↓
G9-03 asset-spec vNext
↓
G9-04 Adapter / Compiler
↓
G9-05 Creator
```

G8-UAT-02 只是提前证明 Stage Playability 所必需的最小 internal slice；不冻结 external asset schema。

## 9. 当前 Next

> **G8-UAT-02 implementation。**
