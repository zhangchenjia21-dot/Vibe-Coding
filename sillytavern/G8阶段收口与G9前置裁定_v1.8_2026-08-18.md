# G8｜阶段收口与 G9 前置裁定 v1.8

状态：`CURRENT DECISION / UAT FIX / RETURN REQUIRED`
日期：2026-08-18

## 1. 当前阶段事实

- G1–G7：PASS / CLOSED。
- G8-WEB-04 Host：PASS / CLOSED。
- G8-WEB-05 Creation Migration：PASS / CLOSED。
- G8-WEB-08 Controlled Multi-action：PASS / CLOSED。
- G8-UAT-01：PASS / CLOSED at `52d0421bc58449ac8763681816bc7a84de93b385`。
- Project Owner Stage UAT rerun：FAIL / BLOCKED。
- G8-UAT-02 implementation：`2c7e6a4cd85e1f3c52350c1b85ae70c99864b940`。
- G8-UAT-02 Independent Review：**FAIL / RETURN REQUIRED**。
- P0：0 confirmed。
- P1：5。
- Project Owner Stage UAT：**NOT AUTHORIZED**。
- G8：**ACTIVE / UAT FIX**。
- G9：**NOT AUTHORIZED**。

当前 review finding：

`G8-UAT-02_IndependentReview_阻塞发现_v1.0_2026-08-18.md`

---

## 2. G8-UAT-02 已确认保留的主干

`2c7e6a4...` 已正确建立：

- Creation Field Semantic Audit；
- configured AI strict semantic materialization；
- Program stable identity / cardinality / placeholder validation；
- concrete opening NPC / Place / Item；
- manual/no-key 不再伪造 generic entity；
- Player Profile structured projection；
- Information Surface 与 generic event journal 分离；
- Runtime JIT Place/NPC；
- Game-local provenance；
- materialization 已进入 Formal Turn SQLite transaction。

因此返修是 narrow return，不重做上述能力。

---

## 3. Independent Review P1

### P1-01 Save/Restore topology revision

动态实体改变 Character/Place/Scene/Connection 集合，但 Canonical Save/Restore 尚未覆盖 Game-local topology revision。必须支持旧存档回滚掉“存档之后才 materialize”的实体与 provenance，并允许 branch divergence。

### P1-02 Materializer activation order

World Materializer 当前几乎每 Turn 在 Semantic 前调用。必须改成 Semantic/Router need-gated activation；普通 observe/dialogue/inner/read-only/wait 无 materialization need 时调用数为 0，materializer failure 不得阻塞 unrelated Turn。

### P1-03 Opening Beat authority

Creation Materializer 当前可直接 author Turn 0 prose，重新产生 Narrative-only concrete entity 风险。必须改成 structured Opening Beat semantic plan → Program refs validation → Narrative Provider authority-safe realization。

### P1-04 Creation private/public boundary

`private_seed` 当前只是 audit metadata；Materializer 输入仍是 flat raw context。必须形成 purpose-built/partitioned materialization context，private source 不得回显到 publicDescription/opening/Product/Narrative。

### P1-05 Item holder/placement

Creation Item plan 缺 holder/location，当前 Adapter 把全部 item 给 player。必须支持 player / character / scene（或等价 typed placement），并由 Program 验证。

---

## 4. G8-UAT-01 成果继续有效

不得回滚：Program Final Outcome authority、No Phantom、location/interactable authority、Bounded != Starved、Player Agency、exactly-five grounded suggestions、G6 Save/Restore、G7 Recovery/Idempotency、WEB-08 controlled multi-action。

---

## 5. 当前关键路径

```text
G8-UAT-01                 PASS / CLOSED
Stage UAT rerun           FAIL
G8-UAT-02 implementation  REVIEWED / RETURN REQUIRED
↓
Narrow P1 return fix
↓
Independent Review rerun
↓
Project Owner Stage UAT
↓
G8 PASS / CLOSED
↓
G9-01 Compatibility Audit
```

---

## 6. G9 Task DAG

顺序不变：

```text
G9-01 Compatibility Audit
↓
G9-02 Runtime Asset Binding
      + Context Orchestration
      + full Game-local Canonical Asset Layer
      + Runtime World Materialization Foundation
↓
G9-03 asset-spec vNext
↓
G9-04 Adapter / Compiler
↓
G9-05 Creator
```

#15 v1.2 Wave 4 同时继续约束：`Runtime Relevant != Model Visible`、cross-owner bounded projection join、Outcome-Gated Continuation Activation。

## 7. 当前 Next

> **G8-UAT-02 narrow return fix → Independent Review rerun。**

未通过重新 Independent Review 前，不授权 Stage UAT，不启动 G9。