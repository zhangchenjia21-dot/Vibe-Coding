# G8｜阶段收口与 G9 前置裁定 v1.8

状态：`CURRENT DECISION / UAT FIX / NARROW RETURN`
日期：2026-08-18

## 1. 当前阶段事实

- G1–G7：PASS / CLOSED。
- G8-WEB-04 / WEB-05 / WEB-08：PASS / CLOSED。
- G8-UAT-01：PASS / CLOSED。
- Project Owner Stage UAT rerun：FAIL / BLOCKED。
- G8-UAT-02 current implementation：`ce6c05ffc89f31a39400f7069c9e7503e4c86d9a`。
- Independent Review rerun：FAIL / NARROW RETURN REQUIRED。
- P0：0 confirmed。
- 原 P1：5；已关闭：4；剩余：1。
- Stage UAT：NOT AUTHORIZED。
- G8：ACTIVE / UAT FIX。
- G9：NOT AUTHORIZED。

当前 finding：`G8-UAT-02_IndependentReview_阻塞发现_v1.0_2026-08-18.md`。

---

## 2. 已关闭的原 P1

`ce6c05f...` 已独立确认：

```text
Save/Restore Game-local topology revision      CLOSED
AI Semantic-gated World Materialization        CLOSED
Structured Opening Beat / Narrative Authority  CLOSED
Creation private/public materialization boundary CLOSED
Typed Item placement core                      CLOSED except one Narrative projection seam
```

Semantic AI 每 Turn 判断 Materialization Need；Program 不做 NLP；World Materializer 仅在 need 成立时调用。

---

## 3. Remaining P1

`scene-present Item` 已能进入 Runtime、SQLite、PlayerSession / Product 与初始 Continuity Context，但 `projectNarrativeSafeCurrentContext()` 最终重投影仍沿用旧 holder 规则，只保留 player / visible Character 持有物，漏掉：

```text
placementKind = scene
holderRef = finalSceneRef
```

因此正式 Narrative 的 `interactableAuthority.itemNames` 可能与 Runtime / Product 不一致。

必须只修这一 seam，并验证移动前后按 finalSceneRef 正确切换 scene-present Item。

---

## 4. 当前关键路径

```text
ce6c05f Independent Review
→ 4 P1 CLOSED / 1 P1 REMAINS
→ scene-item Narrative projection narrow fix
→ Independent Review rerun
→ Project Owner Stage UAT
→ G8 PASS / CLOSED
→ G9-01
```

---

## 5. G9 边界

G9 继续 NOT AUTHORIZED。G9 DAG 仍为 Compatibility Audit → Runtime Asset Binding + Context Orchestration + full Game-local Layer → asset-spec → Adapter → Creator。

#15 v1.2 与 #16 v1.1 继续有效。

## 6. 当前 Next

> **scene-present Item final Narrative-safe projection narrow fix → Independent Review rerun。**
