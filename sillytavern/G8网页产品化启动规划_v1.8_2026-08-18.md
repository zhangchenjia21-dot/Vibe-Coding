# G8｜网页产品化启动规划 v1.8

状态：`ACTIVE / UAT FIX / RETURN REQUIRED`
日期：2026-08-18
当前任务：`G8-UAT-02｜Narrow P1 Return Fix`

## 1. 已完成并保持冻结

- WEB-01 Product UI Contract Freeze：PASS / CLOSED
- WEB-02 Product Shell：PASS / absorbed
- WEB-03 Live Product Session：PASS / CLOSED
- WEB-04 Runtime-extensible UI Host：PASS / CLOSED
- WEB-05 Creation Technical Migration Closure：PASS / CLOSED
- WEB-08 Controlled Multi-action Core Slice：PASS / CLOSED
- G8-UAT-01：PASS / CLOSED at `52d0421...`

这些纵向不整体重做。

---

## 2. G8-UAT-02 当前实现状态

Codex implementation：

`2c7e6a4cd85e1f3c52350c1b85ae70c99864b940`

已确认真实实现：

- Creation Field Semantic Audit；
- configured AI semantic materialization；
- concrete opening NPC / Place / Item；
- placeholder rejection；
- manual/no-key degraded path；
- Player Profile；
- Information Surface semantic correction；
- Runtime JIT Place/NPC；
- Game-local provenance / Formal Turn transaction seam。

Independent Review：

```text
FAIL / RETURN REQUIRED
P0 = 0 confirmed
P1 = 5
Stage UAT = NOT AUTHORIZED
```

Review finding：

`G8-UAT-02_IndependentReview_阻塞发现_v1.0_2026-08-18.md`

---

## 3. 当前 narrow P1 return

必须只针对以下五项返修：

### P1｜Save / Restore topology revision

Save/Restore 必须覆盖动态 Game-local Character/Place/Scene/Connection topology 与 provenance；旧存档恢复时，存档之后才生成的实体必须消失。

### P1｜World Materializer activation

不得所有普通 Turn 都先调用 World Materializer。由 Semantic/Router 判断存在 bounded unresolved local materialization need 后才激活；普通 observe/dialogue/inner/read-only/wait 无 need 时调用数为 0。

### P1｜Opening Beat authority

Materializer 不直接输出可信 final Turn-0 prose。改成 structured opening beat → Program validation → Narrative Provider realization；继续遵守 No Phantom、Player Agency、private disclosure boundary。

### P1｜Creation private/public boundary

private seed 必须从 public materialization path 隔离；新增 private marker zero-disclosure regression。

### P1｜Item holder / placement

Creation Item plan 支持 player / character / scene holder/placement；Program 验证；Inventory 只列 player-held Item。

---

## 4. 不要重做的内容

返修不应重新设计：

- Creation field taxonomy；
- strict Creation Materializer 主体；
- Product Player Profile；
- Information Surface；
- generic placeholder rejection；
- JIT entity stable identity 主方向；
- G8-UAT-01 authority；
- Dynamic Five；
- Host；
- WEB-08。

这是 narrow return，不是新阶段。

---

## 5. 继续禁止

不开始：

- WEB-06 / WEB-07；
- external asset-spec vNext；
- Creator rebuild；
- Game Asset Adapter / Compiler；
- full G9 Context Router；
- speculative whole-world generation；
- arbitrary model DB mutation。

---

## 6. 返修完成 Gate

至少证明：

```text
save-before-materialization → restore-old topology rollback
save-after-materialization → same stable identity
old-save branch → distinct valid future
ordinary turn → 0 world-materializer call
unrelated turn survives materializer failure
structured opening beat → authority-safe Turn 0 narrative
private creation marker → 0 public disclosure
player/NPC/scene-held item placement correct
```

随后重新跑 G5/G6/G7/G8/full + typecheck/lint/build/launcher/disclosure + Real DeepSeek targeted Gate。

---

## 7. 当前阶段边界

```text
G8-UAT-01 = PASS / CLOSED
Stage UAT rerun = FAIL
G8-UAT-02 = REVIEWED / RETURN REQUIRED
G8 = ACTIVE / UAT FIX
Stage UAT = NOT AUTHORIZED
G9 = NOT AUTHORIZED
```

## 8. 当前 Next

> **G8-UAT-02 narrow P1 return fix → Independent Review rerun。**
