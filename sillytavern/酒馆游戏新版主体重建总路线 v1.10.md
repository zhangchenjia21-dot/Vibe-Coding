# 酒馆游戏新版主体重建总路线 v1.10

> 状态：**当前正式路线**  
> 日期：2026-08-18  
> **SUPERSEDES** `酒馆游戏新版主体重建总路线 v1.9.md`。

## 0. 总路线

```text
G1 真实模型兼容性                 PASS / CLOSED
↓
G2 最小世界运行内核               PASS / CLOSED
↓
G3 正式世界能力垂直扩展           PASS / CLOSED
↓
G4 程序判定与 Dice                PASS / CLOSED
↓
G5 长期世界连续性                 PASS / CLOSED
↓
G6 Save / Continue / Restore      PASS / CLOSED
↓
G7 Crash / Resume / Recovery      PASS / CLOSED
↓
G8 网页产品化                     EXIT GATE
↓
G9 资产生态接入                   BLOCKED BY G8 EXIT
↓
G10 Provider 扩展                 FUTURE
↓
G11 Alpha / 全系统验收            FUTURE
↓
G12 Windows Release               FUTURE
```

原则：

> 先证明真实纵向链，再横向扩展。  
> Host Before Protocol。  
> Runtime Context Orchestrator Before Asset Context Schema。

## 1. G8 当前完成状态

### PASS / CLOSED

- WEB-01 Product UI Contract
- WEB-02 Product Shell / Visual Language（absorbed）
- WEB-03 Live Product Session
- WEB-04 Runtime-extensible UI Host Final Convergence
- WEB-05 Creation Product Path
- WEB-05 Technical Migration Closure
- WEB-08 Controlled Multi-action Core Slice

WEB-08 基线：

`sillytavern@3ad5b419e9fb289488a18ded82c5a77f2cdd376c`

已冻结最小 sequence semantics：

```text
one Player Input
→ ordered 2-step deterministic action_sequence
→ Program authorization
→ transient preflight
→ aggregate FormalDelta
→ one atomic commit
→ one Formal Turn
```

任一步需要 Dice / Resolution 时整段在 RNG 前 fail closed；resolved multi-action 不属于 G8。

### DEFERRED / NON-BLOCKING FOR G9

- WEB-06 Save Center / Recovery Product UX
- WEB-07 Undo / Re-input
- deep mobile / WCAG
- advanced Timeline / Archive / multi-device recovery
- Creation Preview
- full Creation Semantic Review
- full Gameplay Recommendation
- UI polish

上述在 G11 / Alpha 前重新拉回。

## 2. 当前唯一 G8 Next

> **G8 Exit Gate**

Exit Gate 不预设新功能实现，只做：

- current main / GitHub-first review；
- Creation → Session；
- Host production vertical；
- multi-action vertical / fail-closed / recovery；
- G5–G7 regression；
- no-key / configured-key；
- product E2E；
- typecheck / lint / build / launcher / disclosure；
- desktop / narrow / keyboard critical path；
- 项目所有者 Stage UAT。

仅 P0/P1 开修复任务。

## 3. G9 当前 Task DAG

G8 CLOSED 后：

```text
G9-01 tavern-asset-spec + tavern-creator Compatibility Audit
↓
G9-02 Runtime Asset Binding + Context Orchestration Foundation
↓
G9-03 asset-spec vNext Machine Contract
↓
G9-04 Game Asset Adapter / Compiler
↓
G9-05 Creator rebuild
↓
Golden Asset Family migration / validation
```

### G9-02 current requirements

第 15 号核心 current = `Runtime Context Orchestration v1.1`。

必须用 internal typed / handwritten profiles 先证明：

```text
Player Input
+ pruned Enabled Package / Feature / Module Directory
+ minimal scene / active context
→ Router Model
→ immediate relevant units / Intent
→ Program structural validation
→ state-mandatory augmentation
→ Runtime Relevant Set
→ bounded JIT Runtime Projection
```

并证明：

```text
Module OFF
→ no routing profile
→ no conditional dependency
→ no module state/surface
```

以及：

```text
background deterministic progression
→ Program only
→ zero model call unless semantic work is actually needed
```

G9-02 仍不冻结 external asset JSON Schema。

## 4. G9-03 之后

asset-spec 只能声明已被 Host / Runtime vertical proof 验证的能力，不得反向发明：

- arbitrary JS / React / DOM / CSS；
- arbitrary state path / expression/query DSL；
- direct Game State mutation；
- Enabled asset full-prompt inclusion；
- 未验证 Host / Context / Action capability。

## 5. 当前状态

```text
WEB-04 PASS
WEB-05 Migration PASS
WEB-08 PASS
↓
G8 Exit Gate ← NOW
↓
G8 CLOSED
↓
G9-01 Compatibility Audit
```
